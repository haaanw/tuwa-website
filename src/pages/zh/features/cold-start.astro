---
import '@fontsource/noto-sans-sc/400.css';
import '@fontsource/noto-sans-sc/700.css';
import { useColdStartTranslations } from '../../../i18n/utils';
import FeaturePageLayout from '../../../layouts/FeaturePageLayout.astro';

const t = useColdStartTranslations('zh');
---

<style is:global>
  html[lang="zh"] { --font-general-sans: 'General Sans', 'Noto Sans SC', system-ui, sans-serif; }
</style>

<FeaturePageLayout
  shapeId="blob-2"
  locale="zh"
  title={t.meta.title}
  description={t.meta.description}
  ogImage="/og/cold-start.png"
  screenshotAlt={t.hero.screenshotAlt}
  outcomeStatement={t.hero.outcomeStatement}
  hookLine={t.hero.hookLine}
>

  <!-- 第一节：工作原理 -->
  <section data-animate class="section-spaced">
    <div class="mx-auto max-w-3xl px-6">
      <h2
        style="
          font-size: var(--text-heading);
          font-weight: var(--weight-heading);
          line-height: var(--leading-heading);
          letter-spacing: var(--tracking-heading);
          color: var(--color-text-1);
          margin-bottom: var(--space-lg);
        "
      >
        {t.howItWorks.heading}
      </h2>

      <p style="margin-top: var(--space-md); font-size: var(--text-body); line-height: var(--leading-body); color: var(--color-text-2);">
        {t.howItWorks.p1}
      </p>

      <p style="margin-top: var(--space-md); font-size: var(--text-body); line-height: var(--leading-body); color: var(--color-text-2);">
        {t.howItWorks.p2}
      </p>

      <h3 style="font-size: var(--text-body); font-weight: var(--weight-heading); color: var(--color-text-1); margin-top: var(--space-xl);">
        {t.howItWorks.day1Heading}
      </h3>

      <p style="margin-top: var(--space-md); font-size: var(--text-body); line-height: var(--leading-body); color: var(--color-text-2);">
        {t.howItWorks.p3}
      </p>

      <h3 style="font-size: var(--text-body); font-weight: var(--weight-heading); color: var(--color-text-1); margin-top: var(--space-xl);">
        {t.howItWorks.days3to5Heading}
      </h3>

      <p style="margin-top: var(--space-md); font-size: var(--text-body); line-height: var(--leading-body); color: var(--color-text-2);">
        {t.howItWorks.p4}
      </p>

      <h3 style="font-size: var(--text-body); font-weight: var(--weight-heading); color: var(--color-text-1); margin-top: var(--space-xl);">
        {t.howItWorks.day7Heading}
      </h3>

      <p style="margin-top: var(--space-md); font-size: var(--text-body); line-height: var(--leading-body); color: var(--color-text-2);">
        {t.howItWorks.p5}
      </p>

      <p style="margin-top: var(--space-md); font-size: var(--text-body); line-height: var(--leading-body); color: var(--color-text-2);">
        {t.howItWorks.p6}
      </p>
    </div>
  </section>

  <!-- 第二节：为什么诚实很重要 -->
  <section
    data-animate
    style="background-color: var(--color-surface); border-top: 1px solid var(--color-divider); border-bottom: 1px solid var(--color-divider);"
  >
    <div
      class="section-spaced mx-auto max-w-3xl px-6"
    >
      <h2
        style="
          font-size: var(--text-heading);
          font-weight: var(--weight-heading);
          line-height: var(--leading-heading);
          letter-spacing: var(--tracking-heading);
          color: var(--color-text-1);
          margin-bottom: var(--space-lg);
        "
      >
        {t.honestySection.heading}
      </h2>

      <p style="margin-top: var(--space-md); font-size: var(--text-body); line-height: var(--leading-body); color: var(--color-text-2);">
        {t.honestySection.p1}
      </p>

      <p style="margin-top: var(--space-md); font-size: var(--text-body); line-height: var(--leading-body); color: var(--color-text-2);">
        {t.honestySection.p2}
      </p>

      <p style="margin-top: var(--space-md); font-size: var(--text-body); line-height: var(--leading-body); color: var(--color-text-2);">
        {t.honestySection.p3}
      </p>
    </div>
  </section>

</FeaturePageLayout>

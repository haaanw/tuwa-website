---
phase: 16-interaction-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
autonomous: true
requirements:
  - IXPN-04

must_haves:
  truths:
    - "All interactive elements have smooth hover transitions using --ease-interactive"
    - "Existing Phase 8 transitions migrated from ease/ease-out to var(--ease-interactive)"
    - "FAQ accordion headers, wheel arcs, and inline links all respond to hover"
    - "prefers-reduced-motion guards wrap all new transitions"
  artifacts:
    - path: "src/styles/global.css"
      provides: "--ease-interactive token and comprehensive hover rules"
      contains: "--ease-interactive: cubic-bezier(0.25, 0.1, 0.25, 1)"
  key_links:
    - from: "src/styles/global.css"
      to: "all interactive elements"
      via: "CSS custom property var(--ease-interactive)"
      pattern: "var\\(--ease-interactive\\)"
---

<objective>
Define the standardized easing token and complete the hover micro-interaction audit across all interactive elements.

Purpose: Every clickable element site-wide gets a smooth, consistent transition using a single easing curve token. This establishes the interaction feel foundation that the magnetic CTA and Lenis integration build upon.
Output: Updated global.css with --ease-interactive token, migrated existing transitions, and new hover rules for uncovered elements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-interaction-polish/16-CONTEXT.md
@.planning/phases/16-interaction-polish/16-RESEARCH.md
@.planning/phases/16-interaction-polish/16-PATTERNS.md
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --ease-interactive token and migrate existing transitions</name>
  <files>src/styles/global.css</files>
  <read_first>src/styles/global.css</read_first>
  <action>
1. Add the easing token to the `:root` block (near the existing shadow/color tokens):
```css
/* Interaction easing — standardized curve for all hover/transitions (D-12) */
--ease-interactive: cubic-bezier(0.25, 0.1, 0.25, 1);
```

2. Migrate existing Phase 8 transitions to use the new token (per D-13):

In `.btn-cta` base (line ~260):
```css
transition: background-color 150ms var(--ease-interactive), box-shadow 150ms var(--ease-interactive);
```

In `.btn-cta` inside `@media (prefers-reduced-motion: no-preference)` (line ~268):
```css
transition: background-color 150ms var(--ease-interactive), box-shadow 150ms var(--ease-interactive), transform 150ms var(--ease-interactive);
```

In `.nav-link` inside `@media (prefers-reduced-motion: no-preference)` (line ~287):
```css
transition: color 100ms var(--ease-interactive);
```

In `.blog-listing-item` inside `@media (prefers-reduced-motion: no-preference)` (line ~300):
```css
transition: box-shadow 200ms var(--ease-interactive);
```

Keep all duration values unchanged — only replace `ease`, `ease-out` with `var(--ease-interactive)`.
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/tuwa-website && grep -c "var(--ease-interactive)" src/styles/global.css | grep -q "^[4-9]\|^[1-9][0-9]" && grep "ease-interactive" src/styles/global.css | head -1 | grep -q "cubic-bezier(0.25, 0.1, 0.25, 1)" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
- global.css contains `--ease-interactive: cubic-bezier(0.25, 0.1, 0.25, 1)` in `:root`
- `.btn-cta` transition uses `var(--ease-interactive)` (not `ease` or `ease-out`)
- `.nav-link` transition uses `var(--ease-interactive)` (not `ease-out`)
- `.blog-listing-item` transition uses `var(--ease-interactive)` (not `ease-out`)
- No occurrences of bare `ease` or `ease-out` in Phase 8 transition declarations remain
  </acceptance_criteria>
  <done>All existing transitions migrated to --ease-interactive token. Zero bare ease/ease-out keywords in interactive element transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Add hover transitions for uncovered interactive elements</name>
  <files>src/styles/global.css</files>
  <read_first>src/styles/global.css</read_first>
  <action>
Add the following new hover rules after the existing Phase 8 hover section (after line ~312, after the `:focus-visible` rule). Per D-09, these are the elements that need coverage:

```css
/* Phase 16 — Hover audit: uncovered interactive elements (D-07, D-09) */

/* FAQ accordion summary hover */
@media (prefers-reduced-motion: no-preference) {
  details > summary {
    cursor: pointer;
    transition: color 150ms var(--ease-interactive);
  }
  details > summary:hover {
    color: var(--color-accent);
  }
}

/* FeatureGrid wheel arc hover — subtle opacity feedback */
@media (prefers-reduced-motion: no-preference) {
  .wheel-arc:not(.is-active) {
    transition: opacity 200ms var(--ease-interactive), fill 200ms var(--ease-interactive);
  }
  .wheel-arc:not(.is-active):hover {
    opacity: 0.75;
  }
}

/* Feature page inline links (not CTA, not nav-link) */
@media (prefers-reduced-motion: no-preference) {
  main a:not(.btn-cta):not(.nav-link):not([data-animate]) {
    transition: color 150ms var(--ease-interactive);
  }
  main a:not(.btn-cta):not(.nav-link):not([data-animate]):hover {
    color: var(--color-accent);
  }
}

/* Footer links — use same nav-link style for consistency */
@media (prefers-reduced-motion: no-preference) {
  footer a:not(.btn-cta) {
    transition: color 150ms var(--ease-interactive);
  }
  footer a:not(.btn-cta):hover {
    color: var(--color-accent);
  }
}
```

Also add transition for `.btn-cta` transform return (needed for magnetic CTA smooth return in Plan 02):
Inside the existing `.btn-cta` `@media (prefers-reduced-motion: no-preference)` block, ensure the transition property includes `transform 300ms var(--ease-interactive)` — this allows the JS-set magnetic transform to animate back smoothly on mouseleave.

Update the `.btn-cta` transition inside the reduced-motion block to:
```css
transition: background-color 150ms var(--ease-interactive), box-shadow 150ms var(--ease-interactive), transform 300ms var(--ease-interactive);
```
(Change transform duration from 150ms to 300ms for the smooth magnetic return animation.)
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/tuwa-website && grep -c "prefers-reduced-motion: no-preference" src/styles/global.css | grep -q "^[7-9]\|^[1-9][0-9]" && grep "details > summary" src/styles/global.css | grep -q "transition" && grep "wheel-arc" src/styles/global.css | grep -q "transition" && grep "footer a" src/styles/global.css | grep -q "transition" && grep "main a:not" src/styles/global.css | grep -q "transition" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
- `details > summary` has `transition: color 150ms var(--ease-interactive)` inside reduced-motion guard
- `.wheel-arc:not(.is-active)` has `transition: opacity 200ms var(--ease-interactive)` inside reduced-motion guard
- `main a:not(.btn-cta):not(.nav-link)` has color transition inside reduced-motion guard
- `footer a:not(.btn-cta)` has color transition inside reduced-motion guard
- All new hover rules produce `:hover` state with `color: var(--color-accent)` or opacity change
- `.btn-cta` transform transition is 300ms (not 150ms) for magnetic return smoothness
- `npx tsc --noEmit` passes (no type errors from CSS changes)
  </acceptance_criteria>
  <done>Every interactive element site-wide has a smooth hover transition. FAQ summaries, wheel arcs, inline links, and footer links all covered with reduced-motion guards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure CSS changes — no user input, no data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | D (Denial of Service) | global.css | accept | CSS-only changes cannot cause DoS; worst case is visual regression |
</threat_model>

<verification>
1. `npx tsc --noEmit` — no TypeScript errors
2. `npm run build` — site builds successfully
3. Visual check: hover any nav link, footer link, FAQ header, or CTA button — all show smooth color/scale transitions
4. Verify reduced-motion: with `prefers-reduced-motion: reduce` set, no transitions fire
</verification>

<success_criteria>
- --ease-interactive token defined and used in 8+ transition declarations
- All previously bare `ease` / `ease-out` in hover transitions replaced
- 4 new element categories have hover transitions (FAQ summary, wheel arc, inline links, footer links)
- All wrapped in prefers-reduced-motion guards
- Site builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-interaction-polish/16-01-SUMMARY.md`
</output>

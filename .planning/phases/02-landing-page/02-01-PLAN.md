---
phase: 02-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.ts
  - src/styles/global.css
  - src/components/Hero.astro
  - src/assets/screenshots/dashboard.png
  - public/badges/app-store-badge.svg
  - package.json
autonomous: true
requirements: [LAND-01, LAND-03, LAND-05]

must_haves:
  truths:
    - "Hero section displays an outcome-led headline differentiating Tuwa for serious athletes"
    - "Hero section shows an app screenshot inside a Dieter Rams device mockup"
    - "Hero section includes App Store badge linking to App Store URL"
    - "Desktop visitors see a QR code alongside the App Store badge in the hero"
    - "Mobile visitors see only the App Store badge (QR hidden)"
    - "Scroll animation CSS is defined with prefers-reduced-motion support"
  artifacts:
    - path: "src/config.ts"
      provides: "APP_STORE_URL constant"
      contains: "export const APP_STORE_URL"
    - path: "src/components/Hero.astro"
      provides: "Hero section component"
      min_lines: 40
    - path: "src/assets/screenshots/dashboard.png"
      provides: "Hero screenshot for Astro Image processing"
    - path: "public/badges/app-store-badge.svg"
      provides: "Official Apple App Store badge"
    - path: "src/styles/global.css"
      provides: "Scroll animation keyframes"
      contains: "data-animate"
  key_links:
    - from: "src/components/Hero.astro"
      to: "src/config.ts"
      via: "import { APP_STORE_URL }"
      pattern: "APP_STORE_URL"
    - from: "src/components/Hero.astro"
      to: "src/assets/screenshots/dashboard.png"
      via: "import dashboardScreenshot"
      pattern: "import.*dashboard"
---

<objective>
Build the Hero section component with device mockup, App Store badge, QR code, and all supporting assets. Also add scroll animation CSS to global.css for use by Wave 2 components.

Purpose: The hero is the first thing visitors see and the primary download conversion element. Setting up config.ts, assets, and animation CSS here unblocks all Wave 2 work.
Output: Hero.astro renders with headline, subtitle, device mockup, App Store badge, and desktop QR code. Scroll animation CSS ready in global.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-landing-page/02-UI-SPEC.md
@.planning/phases/02-landing-page/02-RESEARCH.md
@.planning/phases/02-landing-page/02-PATTERNS.md
@src/styles/global.css
@src/layouts/BaseLayout.astro
@src/components/Header.astro

<interfaces>
<!-- BaseLayout.astro slot contract — content goes inside <main><slot /></main> -->
From src/layouts/BaseLayout.astro:
```typescript
interface Props {
  title: string;
  description: string;
  ogImage?: string;
  canonical?: string;
}
```

<!-- Design tokens from global.css — reference via var(--token), never hardcode -->
Key tokens used by Hero:
- --text-display: 48px, --leading-display: 1.1, --tracking-display: -0.02em
- --text-body: 16px, --leading-body: 1.6
- --space-lg: 24px, --space-xl: 32px, --space-2xl: 48px, --space-3xl: 64px
- --color-text-1, --color-text-2, --color-accent, --color-accent-fg
- --color-divider, --color-bg
- --radius-md: 6px
- --text-label: 13px, --tracking-label: 0.06em
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install qrcode, create config.ts, copy assets, add scroll animation CSS</name>
  <files>package.json, src/config.ts, src/assets/screenshots/dashboard.png, public/badges/app-store-badge.svg, src/styles/global.css</files>
  <read_first>
    - /Users/hanwen/Desktop/tuwa-website/package.json
    - /Users/hanwen/Desktop/tuwa-website/src/styles/global.css
    - /Users/hanwen/Desktop/tuwa-website/.planning/phases/02-landing-page/02-RESEARCH.md (Pattern 1: QR code, Pattern 3: scroll animations)
    - /Users/hanwen/Desktop/tuwa-website/.planning/phases/02-landing-page/02-UI-SPEC.md (Scroll Animations section, Spacing Scale)
  </read_first>
  <action>
1. Install qrcode and its types:
   ```bash
   cd /Users/hanwen/Desktop/tuwa-website && npm install qrcode && npm install --save-dev @types/qrcode
   ```

2. Create `src/config.ts` with the App Store URL constant (same placeholder URL used in Header.astro and Footer.astro):
   ```typescript
   // src/config.ts
   // Single source of truth for the App Store URL.
   // Update when the app is live on the App Store.
   export const APP_STORE_URL = 'https://apps.apple.com/app/tuwa';
   ```

3. Create `src/assets/screenshots/` directory and copy the dashboard screenshot from the app project:
   ```bash
   mkdir -p /Users/hanwen/Desktop/tuwa-website/src/assets/screenshots
   cp "/Users/hanwen/Desktop/Tonus/appstore screenshots/6\"7/Dashboard_framed.png" /Users/hanwen/Desktop/tuwa-website/src/assets/screenshots/dashboard.png
   ```
   Note: Using the framed PNG. Per RESEARCH.md Pitfall 1, when using a framed screenshot, do NOT add a CSS border frame wrapper — use the image directly with just border-radius and overflow:hidden for clean edges.

4. Download the Apple App Store badge SVG (dark variant) to `public/badges/app-store-badge.svg`:
   ```bash
   mkdir -p /Users/hanwen/Desktop/tuwa-website/public/badges
   ```
   Download from https://developer.apple.com/app-store/marketing/guidelines/ — get the "Download on the App Store" black badge SVG. If the download URL is not directly accessible via CLI, create a checkpoint note. As a reliable alternative, use the well-known Apple badge SVG URL or create a minimal SVG placeholder and note it for the user to replace with the official asset.

5. Append scroll animation CSS to the end of `src/styles/global.css` (before no other closing brace — add after the `.tabular-nums` block). Add exactly this CSS:
   ```css
   /* Scroll-triggered reveal animations (Phase 2 — LAND-03) */
   @media (prefers-reduced-motion: no-preference) {
     [data-animate] {
       opacity: 0;
       transform: translateY(16px);
     }
     [data-animate].is-visible {
       animation: fade-up 400ms ease-out forwards;
     }
     @keyframes fade-up {
       to {
         opacity: 1;
         transform: translateY(0);
       }
     }
   }
   @media (prefers-reduced-motion: reduce) {
     [data-animate] {
       opacity: 0;
       transition: opacity 200ms ease-out;
     }
     [data-animate].is-visible {
       opacity: 1;
     }
   }
   ```

6. Run type check: `npx tsc --noEmit` to verify config.ts compiles.
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/tuwa-website && test -f src/config.ts && test -f src/assets/screenshots/dashboard.png && test -f public/badges/app-store-badge.svg && grep -q "APP_STORE_URL" src/config.ts && grep -q "data-animate" src/styles/global.css && grep -q "prefers-reduced-motion" src/styles/global.css && grep -q "qrcode" package.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/config.ts contains `export const APP_STORE_URL = 'https://apps.apple.com/app/tuwa'`
    - src/assets/screenshots/dashboard.png exists (copied from Tonus project)
    - public/badges/app-store-badge.svg exists
    - package.json contains "qrcode" in dependencies
    - package.json contains "@types/qrcode" in devDependencies
    - global.css contains `[data-animate]` selector
    - global.css contains `@keyframes fade-up`
    - global.css contains `@media (prefers-reduced-motion: no-preference)` wrapping the animation
    - global.css contains `@media (prefers-reduced-motion: reduce)` with opacity-only fallback
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>qrcode installed, config.ts exports APP_STORE_URL, screenshot and badge assets in place, scroll animation CSS appended to global.css with reduced-motion support</done>
</task>

<task type="auto">
  <name>Task 2: Create Hero.astro component</name>
  <files>src/components/Hero.astro</files>
  <read_first>
    - /Users/hanwen/Desktop/tuwa-website/src/config.ts (just created — APP_STORE_URL)
    - /Users/hanwen/Desktop/tuwa-website/src/styles/global.css (design tokens + animation CSS)
    - /Users/hanwen/Desktop/tuwa-website/src/components/Header.astro (analog: token usage, responsive hiding, hover patterns)
    - /Users/hanwen/Desktop/tuwa-website/.planning/phases/02-landing-page/02-UI-SPEC.md (Hero Section spec, Copywriting Contract, Accessibility Contract)
    - /Users/hanwen/Desktop/tuwa-website/.planning/phases/02-landing-page/02-PATTERNS.md (Hero.astro pattern assignment)
    - /Users/hanwen/Desktop/tuwa-website/.planning/phases/02-landing-page/02-RESEARCH.md (Pattern 1: QR code, Pattern 2: device mockup, Pattern 5: badge+QR layout)
  </read_first>
  <action>
Create `src/components/Hero.astro` with these exact specifications per D-01, D-02, D-03, D-07:

**Frontmatter:**
```astro
---
import { Image } from 'astro:assets';
import { APP_STORE_URL } from '../config';
import QRCode from 'qrcode';
import dashboardScreenshot from '../assets/screenshots/dashboard.png';

const qrSvg = await QRCode.toString(APP_STORE_URL, {
  type: 'svg',
  width: 120,
  margin: 0,
  color: {
    dark: '#1C1915',
    light: '#00000000'
  }
});
---
```

**Layout structure** (per D-01 — centered layout):
- Outer `<section>` with no `data-animate` (hero is above the fold — do NOT animate per UI-SPEC)
- Inner container: `class="mx-auto text-center"` with `style="max-width: 768px;"`
- Section padding: `padding-top: var(--space-3xl); padding-bottom: var(--space-3xl);` with `class="px-6"`

**Content (top to bottom):**

1. **Headline** (per D-03, UI-SPEC Copywriting Contract):
   - `<h1>` element
   - Text: "Train smarter. Recover with precision."
   - Style: `font-size: var(--text-display); font-weight: 600; line-height: var(--leading-display); letter-spacing: var(--tracking-display); color: var(--color-text-1);`

2. **Subtitle** (gap from headline: `margin-top: var(--space-lg)`):
   - `<p>` element with `style="max-width: 560px;"` and `class="mx-auto"`
   - Text: "Tuwa synthesizes your HRV, sleep, and training load into a single daily readiness score — so you know exactly how hard to push today."
   - Style: `font-size: var(--text-body); line-height: var(--leading-body); color: var(--color-text-2);`

3. **App Store badge + QR code block** (gap from subtitle: `margin-top: var(--space-xl)`):
   - Wrapper: `class="flex flex-col items-center gap-4 sm:flex-row sm:justify-center sm:gap-6"`
   - App Store badge (always visible):
     ```html
     <a href={APP_STORE_URL} rel="noopener noreferrer" aria-label="Download Tuwa on the App Store">
       <img src="/badges/app-store-badge.svg" alt="Download on the App Store" height="44" width="148" loading="eager" />
     </a>
     ```
   - QR code block (desktop only per D-07):
     ```html
     <div class="hidden md:flex flex-col items-center gap-1">
       <div set:html={qrSvg} style="width: 120px; height: 120px; border: 1px solid var(--color-accent); border-radius: var(--radius-sm); padding: 4px;" aria-hidden="true" />
       <span style="font-size: var(--text-label); color: var(--color-text-2); letter-spacing: var(--tracking-label);">SCAN TO DOWNLOAD</span>
     </div>
     ```

4. **Device mockup** (gap from badge block: `margin-top: var(--space-2xl)`):
   - Since using `Dashboard_framed.png` (already has device frame drawn in), do NOT add CSS border frame (per RESEARCH.md Pitfall 1, option 2)
   - Container: `class="relative mx-auto"` with `style="max-width: 320px;"`
   - Use `<Image>` component for optimization:
     ```html
     <Image
       src={dashboardScreenshot}
       alt="Tuwa app showing today's recovery score of 82 — HRV in green zone, sleep 7.5 hours."
       width={320}
       quality={90}
       format="webp"
       loading="eager"
       class="rounded-[44px]"
     />
     ```
   - `loading="eager"` because hero image is above the fold (LCP critical)
   - `class="rounded-[44px]"` to softly round the edges of the framed screenshot image for a clean presentation
   - The `role="img"` and descriptive alt text go on the `<Image>` element itself

No `<style>` block needed — all styles use inline style attributes with CSS custom property references or Tailwind utility classes, following the established Header.astro and Footer.astro patterns.

Run build check after creating: `cd /Users/hanwen/Desktop/tuwa-website && npm run build`
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/tuwa-website && grep -q "Train smarter" src/components/Hero.astro && grep -q "APP_STORE_URL" src/components/Hero.astro && grep -q "QRCode" src/components/Hero.astro && grep -q "dashboardScreenshot" src/components/Hero.astro && grep -q 'hidden md:flex' src/components/Hero.astro && grep -q '<h1' src/components/Hero.astro && grep -q 'loading="eager"' src/components/Hero.astro && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Hero.astro imports from '../config' (APP_STORE_URL)
    - Hero.astro imports QRCode from 'qrcode'
    - Hero.astro imports dashboardScreenshot from '../assets/screenshots/dashboard.png'
    - Hero.astro contains `await QRCode.toString(APP_STORE_URL` in frontmatter
    - Hero.astro contains `<h1` with text "Train smarter. Recover with precision."
    - Hero.astro contains `<p` with subtitle text about HRV, sleep, and training load
    - Hero.astro contains `<a href={APP_STORE_URL}` for badge link
    - Hero.astro contains `<img src="/badges/app-store-badge.svg"` with alt "Download on the App Store"
    - Hero.astro contains `hidden md:flex` for QR code desktop-only visibility
    - Hero.astro contains `set:html={qrSvg}` for inline QR SVG
    - Hero.astro contains `<Image` component with `loading="eager"` and `format="webp"`
    - Hero.astro does NOT contain `data-animate` (hero is above the fold)
    - Hero.astro does NOT contain hardcoded color values (uses var(--token) references)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Hero.astro renders centered hero with headline, subtitle, App Store badge, desktop QR code, and device mockup screenshot. Build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time QR input | App Store URL string passed to qrcode library at build time |
| External asset | Apple App Store badge SVG served as static file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | QR code SVG output | accept | URL is a hardcoded constant in config.ts — no user input; qrcode library generates deterministic SVG from static string at build time |
| T-02-02 | Information Disclosure | App Store badge SVG | accept | Public marketing asset from Apple, no sensitive data |
| T-02-03 | Spoofing | External links | accept | Links go to apps.apple.com — Apple-controlled domain, low risk |
</threat_model>

<verification>
1. `npm run build` succeeds with no errors
2. `npx tsc --noEmit` passes
3. Hero.astro imports APP_STORE_URL from config.ts (single source of truth)
4. QR code generated at build time (no client-side JS)
5. Screenshot processed by Astro Image pipeline (WebP format)
6. Scroll animation CSS in global.css with prefers-reduced-motion
</verification>

<success_criteria>
- Hero section renders with headline, subtitle, badge, QR (desktop), and device mockup
- qrcode package installed and generating SVG at build time
- App Store badge SVG in public/badges/
- Screenshot in src/assets/ for Astro Image processing
- global.css has scroll animation keyframes with reduced-motion media query
- Build and type check both pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-page/02-01-SUMMARY.md`
</output>

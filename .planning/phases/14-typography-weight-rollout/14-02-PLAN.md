---
phase: 14-typography-weight-rollout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/FeaturePageLayout.astro
  - src/layouts/CoachingPageLayout.astro
  - src/layouts/LegalPageLayout.astro
  - src/layouts/BlogPostLayout.astro
  - src/pages/blog/index.astro
  - src/pages/support.astro
  - src/pages/features/recovery-scoring.astro
  - src/pages/features/workload-tracking.astro
  - src/pages/features/smart-templates.astro
  - src/pages/features/cold-start.astro
  - src/pages/features/coaching.astro
  - src/styles/global.css
autonomous: true
requirements: [TYPO-03, TYPO-04, TYPO-05]

must_haves:
  truths:
    - "All page titles (h1) across all 10 pages render at weight 300 (heading token)"
    - "All section headings (h2, h3) on feature pages render at weight 300"
    - "Global CSS selectors (.nav-logo, .btn-cta, .wheel-*) use token vars instead of hardcoded values"
    - "A grep for font-weight numeric literals in src/ returns zero design-weight results (only @font-face range and FaqAccordion ::after exempt)"
  artifacts:
    - path: "src/layouts/FeaturePageLayout.astro"
      provides: "Page title h1 with --weight-heading"
      contains: "var(--weight-heading)"
    - path: "src/layouts/CoachingPageLayout.astro"
      provides: "Page title h1 with --weight-heading"
      contains: "var(--weight-heading)"
    - path: "src/layouts/LegalPageLayout.astro"
      provides: "Page title h1 with --weight-heading"
      contains: "var(--weight-heading)"
    - path: "src/layouts/BlogPostLayout.astro"
      provides: "Post title h1 with --weight-heading"
      contains: "var(--weight-heading)"
    - path: "src/pages/features/coaching.astro"
      provides: "All 6 section headings with --weight-heading"
      contains: "var(--weight-heading)"
    - path: "src/styles/global.css"
      provides: "6 selectors using weight tokens instead of literals"
      contains: "var(--weight-label)"
  key_links:
    - from: "src/layouts/*.astro"
      to: "src/styles/global.css"
      via: "CSS custom property inheritance from :root"
      pattern: "var\\(--weight-heading\\)"
    - from: "src/pages/features/*.astro"
      to: "src/styles/global.css"
      via: "CSS custom property inheritance from :root"
      pattern: "var\\(--weight-heading\\)"
    - from: "src/styles/global.css selectors"
      to: "src/styles/global.css :root tokens"
      via: "Same-file CSS custom property reference"
      pattern: "var\\(--weight-(body|label)\\)"
---

<objective>
Migrate all 4 layout files, 7 page files, and 6 global.css selectors from hardcoded font-weight numeric literals to CSS custom property tokens. This completes the typography weight rollout across all 10 pages, achieving the zero-hardcoded-values goal.

Purpose: Complete the weight contrast system so every page title, section heading, and global UI element uses tokenized weights — enabling single-point weight adjustments from global.css :root.
Output: 12 files migrated, zero hardcoded font-weight design values remaining in src/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-typography-weight-rollout/14-CONTEXT.md
@.planning/phases/14-typography-weight-rollout/14-PATTERNS.md

<interfaces>
<!-- Token definitions from global.css :root (Phase 11, lines 80-83) -->

From src/styles/global.css:
```css
--weight-display: 200;  /* hero headlines 48px+ */
--weight-heading: 300;  /* section titles, page titles */
--weight-body:    500;  /* paragraphs, nav links, descriptions */
--weight-label:   600;  /* micro text, legal, all-caps captions */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate layouts and pages to weight tokens</name>
  <files>src/layouts/FeaturePageLayout.astro, src/layouts/CoachingPageLayout.astro, src/layouts/LegalPageLayout.astro, src/layouts/BlogPostLayout.astro, src/pages/blog/index.astro, src/pages/support.astro, src/pages/features/recovery-scoring.astro, src/pages/features/workload-tracking.astro, src/pages/features/smart-templates.astro, src/pages/features/cold-start.astro, src/pages/features/coaching.astro</files>
  <read_first>
    - src/layouts/FeaturePageLayout.astro
    - src/layouts/CoachingPageLayout.astro
    - src/layouts/LegalPageLayout.astro
    - src/layouts/BlogPostLayout.astro
    - src/pages/blog/index.astro
    - src/pages/support.astro
    - src/pages/features/recovery-scoring.astro
    - src/pages/features/coaching.astro
    - src/pages/features/cold-start.astro
    - .planning/phases/14-typography-weight-rollout/14-PATTERNS.md
  </read_first>
  <action>
Replace hardcoded font-weight values across all layouts and pages per CONTEXT.md D-02:

**4 Layout files** (1 occurrence each, all per D-02):
Each layout has an h1 page title with `font-weight: 600` in an inline style. Replace with `font-weight: var(--weight-heading)` in each:
- `FeaturePageLayout.astro` — h1 inline style `font-weight: 600` -> `font-weight: var(--weight-heading)`
- `CoachingPageLayout.astro` — h1 inline style `font-weight: 600` -> `font-weight: var(--weight-heading)`
- `LegalPageLayout.astro` — h1 inline style `font-weight: 600` -> `font-weight: var(--weight-heading)`
- `BlogPostLayout.astro` — h1 inline style `font-weight: 600` -> `font-weight: var(--weight-heading)`

**blog/index.astro** (2 occurrences, per D-02):
- Page title h1: `font-weight: 600` -> `font-weight: var(--weight-heading)`
- Post listing h2 titles: `font-weight: 600` -> `font-weight: var(--weight-heading)`

**support.astro** (2 occurrences, per D-02 and D-04):
- Contact h2 heading: `font-weight: 600` -> `font-weight: var(--weight-heading)`
- Contact button/link: `font-weight: 600` -> `font-weight: var(--weight-label)` (it is a CTA button, per D-04)

**5 Feature pages** (all h2 and h3 headings -> `--weight-heading` per D-02):
- `recovery-scoring.astro` — 4 occurrences: all `font-weight: 600` on h2/h3 inline styles -> `font-weight: var(--weight-heading)`
- `workload-tracking.astro` — 4 occurrences: same pattern as recovery-scoring
- `smart-templates.astro` — 4 occurrences: same pattern as recovery-scoring
- `cold-start.astro` — use live grep to find ALL occurrences (RESEARCH.md notes 5+ vs UI-SPEC estimate of 4). Replace every `font-weight: 600` on h2/h3 elements with `font-weight: var(--weight-heading)`. Also check for any occurrences in `<style>` blocks.
- `coaching.astro` — 6 occurrences: all `font-weight: 600` in `<style>` blocks for section headings -> `font-weight: var(--weight-heading)`

**IMPORTANT:** All h2 and h3 elements map to `--weight-heading` (300) regardless of their font-size. Some h3 elements use `font-size: var(--text-body)` (16px) — these will appear lighter than surrounding body paragraphs at the same size. This is a locked decision (D-02). The weight contrast is intentional.

After modifying each file, read it back to confirm zero remaining `font-weight: [0-9]` patterns.
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/tuwa-website && grep -rn "font-weight: [0-9]" src/layouts/ src/pages/ --include="*.astro" | grep -v "200 700" ; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 layout files contain `font-weight: var(--weight-heading)` in their h1 inline style, zero `font-weight: 600`
    - blog/index.astro contains `var(--weight-heading)` for both h1 and h2, zero `font-weight: 600`
    - support.astro contains `var(--weight-heading)` for h2 and `var(--weight-label)` for button, zero `font-weight: 600`
    - All 5 feature pages contain only `var(--weight-heading)` for font-weight, zero numeric font-weight literals
    - `grep -rn "font-weight: [0-9]" src/layouts/ src/pages/ --include="*.astro"` returns zero results
    - `npx astro build` completes without errors
  </acceptance_criteria>
  <done>All 11 layout and page files use weight tokens. Every page title h1 renders at weight 300 (heading). Every feature page section heading (h2/h3) renders at weight 300. Support page button uses label weight (600). Zero hardcoded font-weight values in layouts or pages.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate global.css selectors and run final verification</name>
  <files>src/styles/global.css</files>
  <read_first>
    - src/styles/global.css
    - .planning/phases/14-typography-weight-rollout/14-PATTERNS.md (Pattern C: global.css section)
  </read_first>
  <action>
Replace 6 hardcoded font-weight values in global.css selectors with token vars. These selectors stay in global.css but must reference tokens instead of literals.

**Line ~169 — `.nav-logo`:**
`font-weight: 600;` -> `font-weight: var(--weight-label);` (value stays 600, now tokenized)

**Line ~233 — `.nav-dropdown-title`:**
`font-weight: 600;` -> `font-weight: var(--weight-label);` (value stays 600, now tokenized)

**Line ~251 — `.btn-cta`:**
`font-weight: 600;` -> `font-weight: var(--weight-label);` (value stays 600, now tokenized)

**Line ~580 — `.wheel-segment-label`:**
`font-weight: 700;` -> `font-weight: var(--weight-label);` (changes from 700 to 600 — slight weight reduction on SVG all-caps 11px labels)

**Line ~669 — `.wheel-center-title`:**
`font-weight: 600;` -> `font-weight: var(--weight-body);` (changes from 600 to 500 — subtle drop for wheel center panel title)

**Line ~696 — `.wheel-center-cta`:**
`font-weight: 600;` -> `font-weight: var(--weight-label);` (value stays 600, now tokenized. NOTE: UI-SPEC incorrectly labels this as a second `.wheel-center-title` — it is actually `.wheel-center-cta`, confirmed by RESEARCH.md live grep)

**ANTI-PATTERN — DO NOT TOUCH:**
- The `@font-face` declaration `font-weight: 200 700` (structural range, not a design value)
- The `:root` token definitions themselves (`--weight-display: 200;` etc.) — these ARE the source of truth

**After global.css edits, run the full project verification:**
```bash
grep -rn "font-weight: [0-9]" src/ --include="*.astro" --include="*.css"
```
Expected results: only the `@font-face` range line (`font-weight: 200 700`) and the FaqAccordion `::after` decorative glyph (`font-weight: 400`). Both are explicitly exempt.

Also run `npx astro build` to confirm the build passes.
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/tuwa-website && grep -rn "font-weight: [0-9]" src/ --include="*.astro" --include="*.css" | grep -v "200 700" | grep -v "font-weight: 400" ; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - global.css `.nav-logo` contains `font-weight: var(--weight-label)` not `font-weight: 600`
    - global.css `.nav-dropdown-title` contains `font-weight: var(--weight-label)` not `font-weight: 600`
    - global.css `.btn-cta` contains `font-weight: var(--weight-label)` not `font-weight: 600`
    - global.css `.wheel-segment-label` contains `font-weight: var(--weight-label)` not `font-weight: 700`
    - global.css `.wheel-center-title` contains `font-weight: var(--weight-body)` not `font-weight: 600`
    - global.css `.wheel-center-cta` contains `font-weight: var(--weight-label)` not `font-weight: 600`
    - The `@font-face` line `font-weight: 200 700` is preserved exactly as-is
    - The `:root` token definitions (`--weight-display: 200` etc.) are preserved exactly as-is
    - Full grep `font-weight: [0-9]` across src/ returns only exempt lines (@font-face range, ::after 400)
    - `npx astro build` succeeds with no errors
    - `npx tsc --noEmit` passes (per project CLAUDE.md convention)
  </acceptance_criteria>
  <done>All 6 global.css selectors use weight tokens. Full-project grep confirms zero remaining hardcoded font-weight design values (only structural @font-face range and decorative ::after exempt). Build passes. Phase 14 typography weight rollout is complete across all 10 pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Pure CSS refactor within global.css.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02 | Tampering | global.css weight tokens | accept | CSS custom properties are client-side; no sensitive data. Tampering has no security impact. |
</threat_model>

<verification>
After both tasks complete, the full verification gate:
1. `grep -rn "font-weight: [0-9]" src/ --include="*.astro" --include="*.css" | grep -v "200 700" | grep -v "font-weight: 400"` returns zero results
2. `npx astro build` succeeds
3. `npx tsc --noEmit` passes
4. All 21 .astro files + global.css contain `var(--weight-` references where they previously had numeric literals
</verification>

<success_criteria>
- All 4 layout h1 page titles use var(--weight-heading)
- All 7 page files use var(--weight-heading) for h2/h3 headings
- Support page CTA button uses var(--weight-label)
- All 6 global.css selectors use var(--weight-*) tokens
- .wheel-segment-label weight reduced from 700 to 600 via token
- .wheel-center-title weight reduced from 600 to 500 via token
- @font-face range declaration preserved intact
- :root token definitions preserved intact
- Zero hardcoded font-weight design values remain in src/ (only exempt lines)
- Build and type check pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-typography-weight-rollout/14-02-SUMMARY.md`
</output>

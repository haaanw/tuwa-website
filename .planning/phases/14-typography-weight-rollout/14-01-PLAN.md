---
phase: 14-typography-weight-rollout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Hero.astro
  - src/components/StatsCounter.astro
  - src/components/Footer.astro
  - src/components/MobileMenu.astro
  - src/components/FaqAccordion.astro
  - src/components/FeatureGrid.astro
  - src/components/FeatureCTA.astro
  - src/components/LandingCTA.astro
autonomous: true
requirements: [TYPO-03, TYPO-04, TYPO-05]

must_haves:
  truths:
    - "Hero headline is visibly lighter than the subtitle paragraph beneath it (200 vs 500 weight contrast)"
    - "StatsCounter animated numbers appear in ultralight editorial style (200), unmistakably thinner than surrounding text"
    - "Footer section headers read at body weight (500), not as bold as CTA buttons (600)"
    - "Mobile menu nav links display at body weight (500), CTA button at label weight (600)"
    - "FAQ questions (600) read distinctly bolder than FAQ answers (500)"
  artifacts:
    - path: "src/components/Hero.astro"
      provides: "Hero headline with --weight-display token"
      contains: "var(--weight-display)"
    - path: "src/components/StatsCounter.astro"
      provides: "Stat numbers with --weight-display token"
      contains: "var(--weight-display)"
    - path: "src/components/Footer.astro"
      provides: "Footer logo with --weight-label, section headers with --weight-body"
      contains: "var(--weight-body)"
    - path: "src/components/MobileMenu.astro"
      provides: "Nav links with --weight-body, CTA with --weight-label"
      contains: "var(--weight-body)"
    - path: "src/components/FaqAccordion.astro"
      provides: "FAQ h2 with --weight-heading, questions with --weight-label, answers with --weight-body"
      contains: "var(--weight-body)"
    - path: "src/components/FeatureGrid.astro"
      provides: "Section h2 with --weight-heading"
      contains: "var(--weight-heading)"
    - path: "src/components/FeatureCTA.astro"
      provides: "CTA heading with --weight-heading"
      contains: "var(--weight-heading)"
    - path: "src/components/LandingCTA.astro"
      provides: "CTA heading with --weight-heading"
      contains: "var(--weight-heading)"
  key_links:
    - from: "src/components/*.astro"
      to: "src/styles/global.css"
      via: "CSS custom property inheritance from :root"
      pattern: "var\\(--weight-(display|heading|body|label)\\)"
---

<objective>
Migrate all 8 component files from hardcoded font-weight numeric literals to CSS custom property tokens (var(--weight-*)). This covers the most visually impactful changes: Hero headline 600->200, StatsCounter numbers 700->200, and Footer/MobileMenu/FAQ weight adjustments.

Purpose: Apply the weight contrast system to shared components that appear across all pages, establishing the editorial typography feel (light headings, heavier body).
Output: 8 component files with zero hardcoded font-weight values, all using var(--weight-*) tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-typography-weight-rollout/14-CONTEXT.md
@.planning/phases/14-typography-weight-rollout/14-PATTERNS.md

<interfaces>
<!-- Token definitions from global.css :root (Phase 11, lines 80-83) -->
<!-- These are the ONLY four weight tokens. Do not invent others. -->

From src/styles/global.css:
```css
--weight-display: 200;  /* hero headlines 48px+ — maximum editorial lightness */
--weight-heading: 300;  /* section titles, page titles */
--weight-body:    500;  /* paragraphs, nav links, descriptions */
--weight-label:   600;  /* micro text, legal, all-caps captions */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate high-impact components (Hero, StatsCounter, Footer, MobileMenu)</name>
  <files>src/components/Hero.astro, src/components/StatsCounter.astro, src/components/Footer.astro, src/components/MobileMenu.astro</files>
  <read_first>
    - src/components/Hero.astro
    - src/components/StatsCounter.astro
    - src/components/Footer.astro
    - src/components/MobileMenu.astro
    - src/styles/global.css (lines 78-85 for token definitions)
    - .planning/phases/14-typography-weight-rollout/14-PATTERNS.md
  </read_first>
  <action>
Replace hardcoded font-weight values with var(--weight-*) tokens in these 4 components per CONTEXT.md decisions:

**Hero.astro** (1 occurrence, per D-01):
- Find `font-weight: 600` in the hero headline h1 inline style
- Replace with `font-weight: var(--weight-display)` — this changes the rendered weight from 600 to 200 (biggest visual shift)

**StatsCounter.astro** (3 occurrences, per D-06):
- Find all 3 instances of `font-weight: 700` in the stat number span inline styles
- Replace each with `font-weight: var(--weight-display)` — changes from 700 to 200 (most dramatic shift in the codebase)

**Footer.astro** (4 occurrences, per D-04 and D-07):
- Logo link: find `font-weight: 600` in the `<a href="/" aria-label="Tuwa home"` inline style, replace with `font-weight: var(--weight-label)` (value stays 600, now tokenized)
- Section headers "Features", "Resources", "Legal" (3 `<p>` elements): find `font-weight: 600` in each inline style, replace with `font-weight: var(--weight-body)` (drops from 600 to 500)

**MobileMenu.astro** (5 occurrences, per D-08 and D-04, with D-09 class consolidation):
- 4 nav links have identical long inline style strings with `font-weight: 600`. Extract these to a new `.mobile-nav-link` class in the component's own `<style>` block:
  ```css
  .mobile-nav-link {
    padding: var(--space-sm) var(--space-md);
    color: var(--color-text-1);
    text-decoration: none;
    font-size: var(--text-heading);
    font-weight: var(--weight-body);
    border-radius: var(--radius-md);
    min-height: 44px;
    display: flex;
    align-items: center;
  }
  ```
  Then replace each nav link's inline style with `class="block mobile-nav-link"` (keep the existing href and text content)
- CTA link (the "Download" button): find `font-weight: 600` in its inline style, replace with `font-weight: var(--weight-label)` (stays 600, now tokenized). Do NOT extract to a class — CTA has distinct visual treatment.

**IMPORTANT:** Do NOT touch the `font-weight: 400` on the `::after` pseudo-element in FaqAccordion — that is a decorative glyph, not a design weight (handled in Task 2). Do NOT touch any `@font-face` declaration — `font-weight: 200 700` is a structural range, not a design value.
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/tuwa-website && grep -n "font-weight: [0-9]" src/components/Hero.astro src/components/StatsCounter.astro src/components/Footer.astro src/components/MobileMenu.astro | grep -v "200 700" ; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - Hero.astro contains `font-weight: var(--weight-display)` and zero instances of `font-weight: 600`
    - StatsCounter.astro contains `font-weight: var(--weight-display)` (3 instances) and zero instances of `font-weight: 700`
    - Footer.astro contains `font-weight: var(--weight-label)` (1 instance for logo) and `font-weight: var(--weight-body)` (3 instances for section headers), zero instances of `font-weight: 600`
    - MobileMenu.astro contains a `<style>` block with `.mobile-nav-link` class using `font-weight: var(--weight-body)`, CTA uses `font-weight: var(--weight-label)`, zero instances of `font-weight: 600`
    - `npx astro build` completes without errors
  </acceptance_criteria>
  <done>All 4 high-impact components use weight tokens. Hero and StatsCounter show the editorial ultralight style (200). Footer section headers use body weight (500). MobileMenu nav links consolidated into a class with body weight (500).</done>
</task>

<task type="auto">
  <name>Task 2: Migrate remaining components (FaqAccordion, FeatureGrid, FeatureCTA, LandingCTA)</name>
  <files>src/components/FaqAccordion.astro, src/components/FeatureGrid.astro, src/components/FeatureCTA.astro, src/components/LandingCTA.astro</files>
  <read_first>
    - src/components/FaqAccordion.astro
    - src/components/FeatureGrid.astro
    - src/components/FeatureCTA.astro
    - src/components/LandingCTA.astro
    - .planning/phases/14-typography-weight-rollout/14-PATTERNS.md
  </read_first>
  <action>
Replace hardcoded font-weight values in the remaining 4 component files:

**FaqAccordion.astro** (3 design-weight occurrences, per D-02, D-04, D-05):
- `<h2>` FAQ section title inline style: find `font-weight: 600`, replace with `font-weight: var(--weight-heading)` (drops to 300)
- `<summary>` question text inline style: find `font-weight: 600`, replace with `font-weight: var(--weight-label)` (stays 600, now tokenized)
- `<p>` answer text inline style: ADD `font-weight: var(--weight-body)` to the existing inline style string (currently has no font-weight, inheriting ~400; per D-05, bump to explicit 500 body token). Insert it before the closing `"` of the style attribute.
- LEAVE ALONE: `font-weight: 400` on `details > summary::after` — this is a decorative `+`/`-` glyph stroke weight, NOT a design token target. It controls the visual weight of a punctuation character and is explicitly out of scope.

**FeatureGrid.astro** (1 occurrence, per D-02):
- `<h2>` section title inline style: find `font-weight: 600`, replace with `font-weight: var(--weight-heading)` (drops to 300)
- Note: The wheel-segment-label, wheel-center-title, wheel-center-cta classes are in global.css and handled in Plan 02

**FeatureCTA.astro** (1 occurrence, per D-02):
- `<h2>` CTA heading inline style: find `font-weight: 600`, replace with `font-weight: var(--weight-heading)` (drops to 300)

**LandingCTA.astro** (1 occurrence, per D-02):
- `<h2>` CTA heading inline style: find `font-weight: 600`, replace with `font-weight: var(--weight-heading)` (drops to 300)
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/tuwa-website && grep -n "font-weight: [0-9]" src/components/FaqAccordion.astro src/components/FeatureGrid.astro src/components/FeatureCTA.astro src/components/LandingCTA.astro | grep -v "200 700" | grep -v "font-weight: 400" ; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - FaqAccordion.astro contains `var(--weight-heading)` (h2), `var(--weight-label)` (summary), `var(--weight-body)` (answer p), and the `font-weight: 400` on `::after` is preserved untouched
    - FeatureGrid.astro h2 contains `font-weight: var(--weight-heading)`, zero instances of `font-weight: 600`
    - FeatureCTA.astro h2 contains `font-weight: var(--weight-heading)`, zero instances of `font-weight: 600`
    - LandingCTA.astro h2 contains `font-weight: var(--weight-heading)`, zero instances of `font-weight: 600`
    - Grep for `font-weight: [0-9]` across all 8 component files returns only the `::after` 400 line in FaqAccordion (decorative, exempt)
    - `npx astro build` completes without errors
  </acceptance_criteria>
  <done>All 8 components migrated to weight tokens. FAQ has correct 3-tier weight hierarchy (heading 300, label 600, body 500). FeatureGrid, FeatureCTA, LandingCTA section headings all at heading weight (300). Zero hardcoded design weights remain in components (only the exempt decorative ::after 400).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This is a pure CSS refactor with no network, auth, or data changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | CSS custom properties | accept | CSS variables are client-side by nature; no sensitive data in weight tokens. Tampering via DevTools has no security impact. |
</threat_model>

<verification>
After both tasks complete:
1. `grep -rn "font-weight: [0-9]" src/components/ --include="*.astro"` returns only the FaqAccordion `::after` line (font-weight: 400)
2. `npx astro build` succeeds
3. All 8 component files contain `var(--weight-` references
</verification>

<success_criteria>
- All 8 component files use var(--weight-*) tokens instead of hardcoded numeric font-weight values
- Hero headline renders at weight 200 (display token)
- StatsCounter numbers render at weight 200 (display token)
- Footer section headers at 500, logo at 600
- MobileMenu nav links at 500 via consolidated .mobile-nav-link class, CTA at 600
- FAQ: h2 at 300, questions at 600, answers at 500
- FaqAccordion ::after decorative glyph remains at hardcoded 400 (not a design token)
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-typography-weight-rollout/14-01-SUMMARY.md`
</output>

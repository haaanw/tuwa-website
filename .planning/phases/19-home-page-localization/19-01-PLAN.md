---
phase: 19-home-page-localization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/locales/en/home.ts
  - src/i18n/locales/zh/home.ts
  - src/i18n/locales/fr/home.ts
  - src/i18n/utils.ts
  - src/components/Hero.astro
  - src/components/StatsCounter.astro
  - src/components/LandingCTA.astro
  - src/pages/zh/index.astro
  - src/pages/fr/index.astro
  - src/pages/index.astro
autonomous: true
requirements: [I18N-06]

must_haves:
  truths:
    - "Hero component displays Chinese headline/subtitle/aria on /zh/ with no English fragments"
    - "Hero component displays French headline/subtitle/aria on /fr/ with no English fragments"
    - "StatsCounter displays translated stat labels on /zh/ and /fr/"
    - "LandingCTA displays translated headline and supporting copy on /zh/ and /fr/"
    - "Page wrappers pass locale prop to all landing page components"
    - "useTranslations supports home namespace in addition to common"
  artifacts:
    - path: "src/i18n/locales/en/home.ts"
      provides: "English home page translation keys (type source)"
      contains: "hero"
    - path: "src/i18n/locales/zh/home.ts"
      provides: "Chinese home page translations"
      contains: "hero"
    - path: "src/i18n/locales/fr/home.ts"
      provides: "French home page translations"
      contains: "hero"
    - path: "src/i18n/utils.ts"
      provides: "Extended useHomeTranslations function"
      contains: "useHomeTranslations"
    - path: "src/components/Hero.astro"
      provides: "Locale-aware hero with translated text"
      contains: "useHomeTranslations"
    - path: "src/components/StatsCounter.astro"
      provides: "Locale-aware stats with translated labels"
      contains: "useHomeTranslations"
    - path: "src/components/LandingCTA.astro"
      provides: "Locale-aware CTA with translated text"
      contains: "useHomeTranslations"
  key_links:
    - from: "src/pages/zh/index.astro"
      to: "src/components/Hero.astro"
      via: "locale prop passed to Hero"
      pattern: "<Hero locale="
    - from: "src/components/Hero.astro"
      to: "src/i18n/locales/zh/home.ts"
      via: "useHomeTranslations(locale)"
      pattern: "useHomeTranslations"
---

<objective>
Create the home page translation namespace (en/zh/fr), extend the i18n utility to support it, and wire Hero, StatsCounter, and LandingCTA components to render translated content based on locale prop.

Purpose: Proves the per-namespace translation pattern works for page-specific content (per D-02) and delivers 3 of 4 landing page components fully translated.
Output: home.ts files for all locales, extended utils.ts, three translated components, updated page wrappers passing locale.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-home-page-localization/19-CONTEXT.md
@src/i18n/utils.ts
@src/i18n/locales/en/common.ts
@src/components/Hero.astro
@src/components/StatsCounter.astro
@src/components/LandingCTA.astro
@src/pages/zh/index.astro
@src/pages/fr/index.astro
@src/pages/index.astro

<interfaces>
<!-- From src/i18n/utils.ts — current pattern -->
```typescript
import type { Common } from './locales/en/common';
export type Locale = 'en' | 'zh' | 'fr';
export function useTranslations(locale: Locale | undefined): Common;
```

<!-- From src/i18n/locales/en/common.ts — type pattern -->
```typescript
type WidenStrings<T> = T extends string
  ? string
  : T extends readonly (infer U)[]
  ? WidenStrings<U>[]
  : { [K in keyof T]: WidenStrings<T[K]> };

const common = { ... } as const;
export default common;
export type Common = WidenStrings<typeof common>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create home.ts translation files and extend i18n utility</name>
  <files>src/i18n/locales/en/home.ts, src/i18n/locales/zh/home.ts, src/i18n/locales/fr/home.ts, src/i18n/utils.ts</files>
  <read_first>src/i18n/utils.ts, src/i18n/locales/en/common.ts, src/components/Hero.astro, src/components/StatsCounter.astro, src/components/LandingCTA.astro</read_first>
  <action>
**1. Create `src/i18n/locales/en/home.ts`:**

```typescript
type WidenStrings<T> = T extends string
  ? string
  : T extends readonly (infer U)[]
  ? WidenStrings<U>[]
  : { [K in keyof T]: WidenStrings<T[K]> };

const home = {
  hero: {
    headline: 'Train smarter. Recover with precision.',
    subtitle: 'Tuwa combines HRV, sleep, training load, and six fatigue dimensions into a single readiness score — so you know exactly how hard to push today.',
    deviceAlt: 'Tuwa app showing today\'s recovery score of 82 — HRV in green zone, sleep 7.5 hours.',
    badgeAlt: 'Download on the App Store',
    badgeAriaLabel: 'Download Tuwa on the App Store',
  },
  stats: {
    athletes: 'Athletes using Tuwa',
    sessions: 'Sessions analyzed',
    accuracy: 'HRV prediction accuracy',
  },
  cta: {
    headline: 'Built for athletes who take training seriously.',
    body: 'Evidence-based workload management. No vanity metrics. No noise.',
  },
  featureGrid: {
    heading: 'Everything you need to train without guessing',
    features: [
      {
        title: 'Recovery Scoring',
        desc: 'HRV, sleep, heart rate, and training context synthesized into one daily readiness number.',
        href: '/features/recovery-scoring',
      },
      {
        title: 'Workload Tracking',
        desc: 'Multi-factor fatigue tracking and load spike detection keep you in the optimal zone.',
        href: '/features/workload-tracking',
      },
      {
        title: 'Smart Templates',
        desc: 'Prescribed workouts with target sets, weight, and rep goals built in.',
        href: '/features/smart-templates',
      },
      {
        title: 'Cold-Start Onboarding',
        desc: 'Tuwa starts useful on day one — no weeks of baseline data required.',
        href: '/features/cold-start',
      },
      {
        title: 'Coach + Athlete',
        desc: 'Real-time recovery visibility and prescribed workouts for your whole team.',
        href: '/features/coaching',
      },
    ],
    segmentLabels: ['RECOVERY', 'LOAD', 'TRAINING', 'ONBOARDING', 'COACHING'],
    exploreCta: 'Explore',
  },
} as const;

export default home;
export type Home = WidenStrings<typeof home>;
```

**2. Create `src/i18n/locales/zh/home.ts`:**

```typescript
import type { Home } from '../en/home';

const home: Home = {
  hero: {
    headline: '科学训练，精准恢复。',
    subtitle: 'Tuwa 将心率变异、睡眠、训练负荷和六维疲劳指标融合为一个准备状态评分——让你每天都知道该练多狠。',
    deviceAlt: 'Tuwa 应用显示今日恢复评分 82 分——心率变异处于绿色区间，睡眠 7.5 小时。',
    badgeAlt: '在 App Store 下载',
    badgeAriaLabel: '在 App Store 下载 Tuwa',
  },
  stats: {
    athletes: '运动员正在使用 Tuwa',
    sessions: '训练课已分析',
    accuracy: 'HRV 预测准确率',
  },
  cta: {
    headline: '为认真对待训练的运动员而造。',
    body: '基于证据的负荷管理。没有花哨数据，没有干扰噪音。',
  },
  featureGrid: {
    heading: '训练不再靠猜测',
    features: [
      {
        title: '恢复评分',
        desc: '将心率变异、睡眠、心率和训练上下文综合为一个每日准备状态数值。',
        href: '/features/recovery-scoring',
      },
      {
        title: '负荷追踪',
        desc: '多因素疲劳追踪与负荷激增检测，让你始终处于最佳训练区间。',
        href: '/features/workload-tracking',
      },
      {
        title: '智能模板',
        desc: '内置目标组数、重量和次数的训练计划。',
        href: '/features/smart-templates',
      },
      {
        title: '冷启动方案',
        desc: '第一天就能用——无需数周基线数据积累。',
        href: '/features/cold-start',
      },
      {
        title: '教练 + 运动员',
        desc: '实时查看全队恢复状态，一键下发训练计划。',
        href: '/features/coaching',
      },
    ],
    segmentLabels: ['恢复', '负荷', '训练', '入门', '教练'],
    exploreCta: '了解详情',
  },
};

export default home;
```

**3. Create `src/i18n/locales/fr/home.ts`:**

```typescript
import type { Home } from '../en/home';

const home: Home = {
  hero: {
    headline: 'Entraîne-toi plus intelligemment. Récupère avec précision.',
    subtitle: 'Tuwa combine VFC, sommeil, charge d\'entraînement et six dimensions de fatigue en un seul score de forme — pour savoir exactement à quelle intensité pousser aujourd\'hui.',
    deviceAlt: 'Application Tuwa affichant un score de récupération de 82 — VFC en zone verte, 7,5 heures de sommeil.',
    badgeAlt: 'Télécharger sur l\'App Store',
    badgeAriaLabel: 'Télécharger Tuwa sur l\'App Store',
  },
  stats: {
    athletes: 'Athlètes utilisent Tuwa',
    sessions: 'Séances analysées',
    accuracy: 'Précision de prédiction VFC',
  },
  cta: {
    headline: 'Conçu pour les athlètes qui prennent l\'entraînement au sérieux.',
    body: 'Gestion de la charge basée sur les preuves. Pas de métriques de vanité. Pas de bruit.',
  },
  featureGrid: {
    heading: 'Tout ce qu\'il te faut pour ne plus t\'entraîner au hasard',
    features: [
      {
        title: 'Score de récupération',
        desc: 'VFC, sommeil, fréquence cardiaque et contexte d\'entraînement synthétisés en un score de forme quotidien.',
        href: '/features/recovery-scoring',
      },
      {
        title: 'Suivi de charge',
        desc: 'Suivi multi-facteurs de la fatigue et détection des pics de charge pour rester en zone optimale.',
        href: '/features/workload-tracking',
      },
      {
        title: 'Modèles intelligents',
        desc: 'Séances prescrites avec séries, poids et objectifs de répétitions intégrés.',
        href: '/features/smart-templates',
      },
      {
        title: 'Démarrage immédiat',
        desc: 'Tuwa est utile dès le premier jour — pas besoin de semaines de données de référence.',
        href: '/features/cold-start',
      },
      {
        title: 'Coach + Athlète',
        desc: 'Visibilité en temps réel sur la récupération et séances prescrites pour toute ton équipe.',
        href: '/features/coaching',
      },
    ],
    segmentLabels: ['RÉCUPÉRATION', 'CHARGE', 'ENTRAÎNEMENT', 'DÉMARRAGE', 'COACHING'],
    exploreCta: 'Explorer',
  },
};

export default home;
```

**4. Extend `src/i18n/utils.ts`:**

Add home namespace imports and a new `useHomeTranslations` function. Keep the existing `useTranslations` unchanged:

```typescript
import enCommon from './locales/en/common';
import zhCommon from './locales/zh/common';
import frCommon from './locales/fr/common';
import enHome from './locales/en/home';
import zhHome from './locales/zh/home';
import frHome from './locales/fr/home';
import type { Common } from './locales/en/common';
import type { Home } from './locales/en/home';

export type Locale = 'en' | 'zh' | 'fr';
export type CommonTranslations = Common;
export type HomeTranslations = Home;

const translations: Record<Locale, Common> = {
  en: enCommon,
  zh: zhCommon,
  fr: frCommon,
};

const homeTranslations: Record<Locale, Home> = {
  en: enHome,
  zh: zhHome,
  fr: frHome,
};

export function useTranslations(locale: Locale | undefined): Common {
  return translations[locale ?? 'en'] ?? translations['en'];
}

export function useHomeTranslations(locale: Locale | undefined): Home {
  return homeTranslations[locale ?? 'en'] ?? homeTranslations['en'];
}
```
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/tuwa-website && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
- File src/i18n/locales/en/home.ts exists and contains `hero:` with `headline` key
- File src/i18n/locales/zh/home.ts exists and contains `headline: '科学训练，精准恢复。'`
- File src/i18n/locales/fr/home.ts exists and contains `headline: 'Entraîne-toi plus intelligemment`
- File src/i18n/utils.ts contains `export function useHomeTranslations`
- File src/i18n/utils.ts contains `import enHome from './locales/en/home'`
- File src/i18n/utils.ts contains `import type { Home } from './locales/en/home'`
- `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Three home.ts locale files exist with matching structure, utils.ts exports useHomeTranslations, TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Hero, StatsCounter, LandingCTA and update page wrappers</name>
  <files>src/components/Hero.astro, src/components/StatsCounter.astro, src/components/LandingCTA.astro, src/pages/zh/index.astro, src/pages/fr/index.astro, src/pages/index.astro</files>
  <read_first>src/components/Hero.astro, src/components/StatsCounter.astro, src/components/LandingCTA.astro, src/pages/zh/index.astro, src/pages/fr/index.astro, src/pages/index.astro, src/i18n/utils.ts</read_first>
  <action>
**1. Modify `src/components/Hero.astro`:**

Add to frontmatter (before the closing `---`):
```typescript
import { useHomeTranslations } from '../i18n/utils';
import type { Locale } from '../i18n/utils';

interface Props {
  locale?: Locale;
}

const { locale = 'en' } = Astro.props;
const t = useHomeTranslations(locale);
```

Replace hardcoded text in template:
- `Train smarter. Recover with precision.` -> `{t.hero.headline}`
- `Tuwa combines HRV, sleep, training load, and six fatigue dimensions into a single readiness score — so you know exactly how hard to push today.` -> `{t.hero.subtitle}`
- `alt="Tuwa app showing today's recovery score of 82 — HRV in green zone, sleep 7.5 hours."` -> `alt={t.hero.deviceAlt}`
- `aria-label="Download Tuwa on the App Store"` -> `aria-label={t.hero.badgeAriaLabel}`
- `alt="Download on the App Store"` -> `alt={t.hero.badgeAlt}`

**2. Modify `src/components/StatsCounter.astro`:**

Replace the comment `// No props — metrics are hardcoded per D-10` with:
```typescript
import { useHomeTranslations } from '../i18n/utils';
import type { Locale } from '../i18n/utils';

interface Props {
  locale?: Locale;
}

const { locale = 'en' } = Astro.props;
const t = useHomeTranslations(locale);
```

Replace hardcoded stat labels:
- `Athletes using Tuwa` -> `{t.stats.athletes}`
- `Sessions analyzed` -> `{t.stats.sessions}`
- `HRV prediction accuracy` -> `{t.stats.accuracy}`

Keep the numeric values (1,200+, 85,000+, 94%) and `data-counter-*` attributes unchanged — numbers are universal.

**3. Modify `src/components/LandingCTA.astro`:**

Add frontmatter section (component currently has no frontmatter):
```astro
---
import { useHomeTranslations } from '../i18n/utils';
import type { Locale } from '../i18n/utils';

interface Props {
  locale?: Locale;
}

const { locale = 'en' } = Astro.props;
const t = useHomeTranslations(locale);
---
```

Replace hardcoded text:
- `Built for athletes who take training seriously.` -> `{t.cta.headline}`
- `Evidence-based workload management. No vanity metrics. No noise.` -> `{t.cta.body}`

**4. Update page wrappers to pass locale:**

`src/pages/zh/index.astro` — change component calls:
```astro
<Hero locale="zh" />
<FeatureGrid locale="zh" />
<StatsCounter locale="zh" />
<LandingCTA locale="zh" />
```

`src/pages/fr/index.astro` — change component calls:
```astro
<Hero locale="fr" />
<FeatureGrid locale="fr" />
<StatsCounter locale="fr" />
<LandingCTA locale="fr" />
```

`src/pages/index.astro` — examine if it uses these components. If so, no change needed (components default to 'en'). If it does NOT pass locale, leave as-is since the default handles English.
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/tuwa-website && npm run build</automated>
  </verify>
  <acceptance_criteria>
- src/components/Hero.astro contains `useHomeTranslations` import
- src/components/Hero.astro contains `{t.hero.headline}` (no hardcoded English headline)
- src/components/Hero.astro does NOT contain the literal string `Train smarter. Recover with precision.`
- src/components/StatsCounter.astro contains `useHomeTranslations` import
- src/components/StatsCounter.astro contains `{t.stats.athletes}` (no hardcoded English label)
- src/components/StatsCounter.astro does NOT contain `Athletes using Tuwa`
- src/components/LandingCTA.astro contains `useHomeTranslations` import
- src/components/LandingCTA.astro does NOT contain `Built for athletes who take training seriously.`
- src/pages/zh/index.astro contains `<Hero locale="zh" />`
- src/pages/zh/index.astro contains `<StatsCounter locale="zh" />`
- src/pages/zh/index.astro contains `<LandingCTA locale="zh" />`
- src/pages/fr/index.astro contains `<Hero locale="fr" />`
- `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Hero, StatsCounter, LandingCTA render translated content on /zh/ and /fr/. Page wrappers pass locale to all components. Build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. All translation content is build-time static. No user input processed. Locale is determined by URL path (Astro routing), not query parameters or cookies.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information Disclosure | home.ts translation files | accept | Translation files contain only marketing copy — no secrets, no PII. Public by nature. |
| T-19-02 | Tampering | locale prop | accept | Prop is hardcoded in page wrapper files at build time ("zh" or "fr"), not derived from user input at runtime |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes — TypeScript catches structural mismatches between locale files
2. `npm run build` succeeds — Astro validates all component props and expressions
3. grep for hardcoded English in Hero/StatsCounter/LandingCTA shows zero matches
</verification>

<success_criteria>
- Hero, StatsCounter, and LandingCTA render locale-specific text via useHomeTranslations()
- zh/fr page wrappers pass locale="zh" or locale="fr" to all components
- English pages unchanged (locale defaults to 'en')
- TypeScript and build pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/19-home-page-localization/19-01-SUMMARY.md`
</output>

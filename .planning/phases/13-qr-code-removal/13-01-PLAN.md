---
phase: 13-qr-code-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/LandingCTA.astro
  - package.json
autonomous: true
requirements:
  - CTA-01
  - CTA-02
  - CTA-03

must_haves:
  truths:
    - "QR code does not appear on the landing page"
    - "App Store badge does not appear in the LandingCTA section"
    - "Header CTA button navigates to App Store"
    - "Hero App Store badge navigates to App Store"
    - "Footer App Store badge navigates to App Store"
    - "No layout shift after removal (CLS = 0)"
  artifacts:
    - path: "src/components/LandingCTA.astro"
      provides: "Clean closing statement without QR or badge"
      contains: "Built for athletes who take training seriously"
    - path: "package.json"
      provides: "Clean dependency list without qrcode"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/LandingCTA.astro"
      via: "component import"
      pattern: "LandingCTA"
---

<objective>
Remove the QR code block and adjacent App Store badge from LandingCTA, remove the qrcode npm dependency, and verify remaining CTAs still work.

Purpose: The landing page has three other download CTAs (header, hero, footer). The LandingCTA section's QR code and badge are redundant clutter. Removing them makes the section a clean closing statement.
Output: Simplified LandingCTA.astro with headline + copy only, qrcode dependency removed from package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-qr-code-removal/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip QR code, badge, and imports from LandingCTA.astro</name>
  <files>src/components/LandingCTA.astro</files>
  <read_first>
    - src/components/LandingCTA.astro
    - src/components/Hero.astro
    - src/components/Footer.astro
  </read_first>
  <action>
Rewrite src/components/LandingCTA.astro to remove:

1. The entire frontmatter block (lines 1-14) -- delete the `import { APP_STORE_URL }` line (per D-04), the `import QRCode` line (per D-03), and the `qrSvg` generation code. The component needs NO frontmatter at all after this.

2. The entire badge+QR flex container (per D-02) -- delete the div with class `flex flex-col items-center gap-4 sm:flex-row sm:justify-center sm:gap-6` and everything inside it (lines 48-82). This removes both the App Store badge link and the QR code block.

Keep (per D-01):
- The outer `<section>` with `data-animate` attribute and surface background styling
- The `section-spaced mx-auto max-w-6xl px-6` container div
- The `mx-auto text-center` inner div with `max-width: 640px`
- The h2 heading "Built for athletes who take training seriously."
- The supporting paragraph "Evidence-based workload management. No vanity metrics. No noise."

Spacing adjustment (Claude's discretion): Add `padding-bottom: var(--space-xl);` to the supporting paragraph's style to give the section a balanced bottom weight now that the badge+QR block is gone. Alternatively, adjust the section-spaced padding if it looks unbalanced -- the key constraint is the section should feel visually complete as a standalone closing statement.

The resulting file should be a simple Astro component with NO frontmatter fence (no `---` blocks), just the HTML template.
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/tuwa-website && grep -c "qrcode\|QRCode\|qrSvg\|APP_STORE_URL\|app-store-badge" src/components/LandingCTA.astro | grep "^0$" && grep -c "Built for athletes" src/components/LandingCTA.astro | grep "^1$" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/LandingCTA.astro contains NO import statements (no frontmatter fence)
    - src/components/LandingCTA.astro contains NO reference to "qrcode", "QRCode", "qrSvg", "APP_STORE_URL", or "app-store-badge"
    - src/components/LandingCTA.astro contains "Built for athletes who take training seriously."
    - src/components/LandingCTA.astro contains "Evidence-based workload management"
    - src/components/LandingCTA.astro contains "data-animate"
    - src/components/LandingCTA.astro contains "section-spaced"
  </acceptance_criteria>
  <done>LandingCTA.astro is a clean closing statement section with headline and supporting copy only. No QR code, no App Store badge, no imports.</done>
</task>

<task type="auto">
  <name>Task 2: Remove qrcode dependency and verify build</name>
  <files>package.json</files>
  <read_first>
    - package.json
  </read_first>
  <action>
Run the following commands in sequence:

1. `npm uninstall qrcode` -- removes `qrcode` from dependencies in package.json and package-lock.json (per D-05)
2. `npm uninstall @types/qrcode` -- removes `@types/qrcode` from devDependencies
3. `npm run build` -- full Astro production build to confirm:
   - No import errors from removed qrcode package
   - LandingCTA.astro compiles without the removed imports
   - All other pages build successfully
   - Zero build warnings related to the removal

If the build fails, the most likely cause is a lingering qrcode import somewhere. Check the error message and fix.

After successful build, verify CTA links are present in the built output:
- `grep -r "apps.apple.com" dist/index.html` should return matches (hero and footer badges still reference APP_STORE_URL)
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/tuwa-website && grep -c '"qrcode"' package.json | grep "^0$" && grep -c '"@types/qrcode"' package.json | grep "^0$" && npm run build 2>&1 | tail -5 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - package.json does NOT contain "qrcode" in dependencies
    - package.json does NOT contain "@types/qrcode" in devDependencies
    - `npm run build` exits with code 0
    - dist/index.html contains "apps.apple.com" (hero and footer CTAs intact)
    - dist/index.html does NOT contain "SCAN TO DOWNLOAD" (QR label gone)
  </acceptance_criteria>
  <done>qrcode and @types/qrcode removed from package.json. Production build succeeds. Hero and footer App Store CTAs confirmed present in built output. QR code content confirmed absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan only removes code.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | LandingCTA.astro | accept | Removal-only change, no new inputs or logic. Existing CSP and static build provide integrity. |
</threat_model>

<verification>
1. `npm run build` succeeds with zero errors
2. `grep -r "qrcode\|QRCode" src/` returns zero results
3. `grep "apps.apple.com" dist/index.html` returns matches (hero + footer CTAs)
4. Visual check: landing page loads, no layout jump, closing statement section visible
</verification>

<success_criteria>
- QR code and App Store badge completely removed from LandingCTA
- qrcode npm package removed from project
- Build passes cleanly
- Header, hero, and footer CTAs remain functional in built output
</success_criteria>

<output>
After completion, create `.planning/phases/13-qr-code-removal/13-01-SUMMARY.md`
</output>

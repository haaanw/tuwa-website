---
phase: 11
slug: css-foundation-token-system
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-15
---

# Phase 11 — UI Design Contract

> Visual and interaction contract for Phase 11: CSS Foundation & Token System.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.
>
> **Phase nature:** This is a CSS infrastructure phase. The explicit success criterion
> is NO visible change to the current design. All contracts below preserve what already
> exists and define new tokens/scaffolding that future phases will activate.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (no shadcn — Astro project) |
| Preset | not applicable |
| Component library | none |
| Icon library | none |
| Font | General Sans variable (Fontshare, self-hosted WOFF2 via Astro Font API) |

**Font configuration change in this phase:**
Current `astro.config.mjs` declares `weights: ["400", "600"]` (two discrete files).
Phase 11 changes this to `weights: "200 700"` — a single WOFF2 covering the full
variable axis. The CSS variable `--font-general-sans` stays identical; only the
network payload changes.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| --space-xs | 4px | Icon gaps, inline padding |
| --space-sm | 8px | Compact element spacing |
| --space-md | 16px | Default element spacing |
| --space-lg | 24px | Section padding |
| --space-xl | 32px | Layout gaps |
| --space-2xl | 48px | Major section breaks |
| --space-3xl | 64px | Page-level spacing |
| --space-section-mobile | 72px | Section vertical padding (mobile) |
| --space-section-desktop | 128px | Section vertical padding (desktop) |

Source: existing tokens in `src/styles/global.css` :root, lines 29–65.
No changes to spacing in this phase. Exceptions: none.

---

## Typography

### Existing Type Scale (unchanged, carried forward)

| Role | Size | Line Height | Letter Spacing |
|------|------|-------------|----------------|
| Display | 48px (clamp 32–48px on hero) | 1.1 | -0.02em |
| Heading | 28px | 1.2 | -0.01em |
| Body | 16px | 1.6 | — |
| Label | 13px | 1.4 | 0.06em (all-caps contexts) |

Source: `--text-display`, `--text-heading`, `--text-body`, `--text-label` in global.css.

### New Weight Tokens (Phase 11 deliverable)

These four tokens are DEFINED in this phase but NOT applied yet (Phase 14 applies them).
Current hardcoded `font-weight` values across pages remain untouched until Phase 14.

| Token | Value | Intended Role |
|-------|-------|---------------|
| --weight-display | 200 | Ultralight — hero headlines 48px+ |
| --weight-heading | 300 | Light — section titles, page titles |
| --weight-body | 500 | Medium — paragraphs, nav links, descriptions |
| --weight-label | 600 | Semi-bold — micro text, legal, all-caps captions |

Source: Decisions D-01 through D-05 in `11-CONTEXT.md`.

Weight gradient rationale: 200 → 300 → 500 → 600 creates maximum editorial contrast
at display scale (large+light vs small+heavy). This is an inversion of conventional
weight hierarchy — intentional for the Tuwa brand voice.

**Current font weights in use (not changed in Phase 11):**
Existing hardcoded values of `font-weight: 600` and `font-weight: 700` throughout
source files remain as-is. The `--weight-*` tokens are scaffolded, not activated.

---

## Color

No color changes in this phase. Carrying forward established system.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #F4F1ED | Page background — warm travertine off-white |
| Secondary (30%) | #EDEAE6 | Nav bar, footer, cards |
| Accent (10%) | #2B5240 | CTA buttons only (current phase — unchanged) |
| Destructive | #6E3A3A | Destructive actions only |

Source: `--color-bg`, `--color-surface`, `--color-accent`, `--color-destructive`
in `src/styles/global.css` lines 10–28.

Accent reserved for: CTA buttons (`.btn-cta`), focus rings (`:focus-visible`),
active wheel segments (`.wheel-arc.is-active`), active nav links on hover.

### Matisse Color Scaffolding (Phase 11 — placeholder only)

`.matisse-frieze` and `.matisse-shape` classes are scaffolded in this phase.
Color is stubbed to `--color-accent` (#2B5240 forest green) as the first-attempt value.
If green does not read well on travertine in Phase 15 visual review, charcoal
(#1C1915) is the declared fallback. Phase 15 makes the final call.

No color tokens are added to `:root` for Matisse in this phase — the class stubs
use `var(--color-accent)` directly as a provisional reference.

---

## View Transition Contract

This section is specific to Phase 11's IXPN-01 and IXPN-02 deliverables.

### Implementation

```css
@view-transition {
  navigation: auto;
}

::view-transition-old(root),
::view-transition-new(root) {
  animation-duration: 200ms;
  animation-timing-function: ease-out;
}
```

| Property | Value | Source |
|----------|-------|--------|
| Mechanism | Native CSS `@view-transition { navigation: auto }` | Decision D-06 |
| Duration | 200ms | Decision D-07 |
| Easing | ease-out | Claude's discretion — smooth deceleration at crossfade end |
| Scope | Global crossfade only | Decision D-08 |
| Named transitions | None in this phase | Decision D-08 |
| Astro component | NOT used (`<ClientRouter>` / `<ViewTransitions />` excluded) | Decision D-06 |

**IntersectionObserver compatibility (IXPN-02):**
The existing IO scroll-reveal in `BaseLayout.astro` uses `astro:page-load` event
pattern. The native `@view-transition` approach does not fire `astro:after-swap`,
so the existing IO observer wiring is compatible without modification. No migration
required in this phase. Verify by navigating between pages and confirming
`[data-animate]` elements re-reveal on the new page.

**Reduced-motion guard:**
```css
@media (prefers-reduced-motion: reduce) {
  ::view-transition-old(root),
  ::view-transition-new(root) {
    animation-duration: 0.01ms;
  }
}
```

---

## Matisse CSS Scaffolding Contract

Phase 11 delivers empty placeholder classes only. No visual output.

```css
/* Phase 11 scaffold — visual properties added in Phase 15 */
.matisse-frieze {
  position: relative;
  overflow: hidden;
}

.matisse-shape {
  position: absolute;
  fill: var(--color-accent);
}

@media (prefers-reduced-motion: no-preference) {
  .matisse-shape {
    /* animation properties added in Phase 15 */
  }
}
```

| Class | Purpose | Phase that populates it |
|-------|---------|------------------------|
| `.matisse-frieze` | Wrapper for the full-width hero frieze band | Phase 15 |
| `.matisse-shape` | Individual biomorphic SVG cut-out element | Phase 15 |

`prefers-reduced-motion` guard is included from the start (consistent with 10+
existing instances in global.css). Source: Decision D-10.

---

## Copywriting Contract

Phase 11 introduces no user-visible UI, no new pages, no new interactive elements.
No copywriting changes are needed in this phase.

| Element | Copy |
|---------|------|
| Primary CTA | "Download on the App Store" (unchanged — not touched in this phase) |
| Empty state | Not applicable — no new UI surfaces |
| Error state | Not applicable — no new UI surfaces |
| Destructive confirmation | Not applicable — no destructive actions |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — Astro project, no shadcn |
| Third-party | none | not applicable |

No third-party registries, blocks, or packages are added in this phase.

---

## Success Criteria Mapping

| Criterion | Contract Requirement |
|-----------|---------------------|
| Network tab shows single WOFF2 for weights 200–700 | `weights: "200 700"` range syntax in astro.config.mjs |
| DevTools shows `--weight-display`, `--weight-heading`, `--weight-body`, `--weight-label` in `:root` | Four tokens added to `:root` block in global.css |
| Navigating between pages produces visible crossfade in Chrome 126+ / Safari 18.2+ | `@view-transition { navigation: auto }` + 200ms ease-out |
| IO scroll-reveal still fires after navigation | Native view-transition does not interfere with existing IO pattern |
| `.matisse-frieze` and `.matisse-shape` exist with `prefers-reduced-motion` guard | Scaffold classes present, reduced-motion guard in place |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

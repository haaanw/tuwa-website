---
phase: 21-legal-support-blog
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - src/pages/zh/privacy.astro
  - src/pages/fr/privacy.astro
  - src/pages/zh/terms.astro
  - src/pages/fr/terms.astro
  - src/pages/zh/support.astro
  - src/pages/fr/support.astro
  - src/pages/zh/blog/index.astro
  - src/pages/fr/blog/index.astro
autonomous: true
requirements:
  - I18N-09
  - I18N-10

must_haves:
  truths:
    - "Privacy page is available at /zh/privacy and /fr/privacy with fully translated content"
    - "Terms page is available at /zh/terms and /fr/terms with fully translated content"
    - "Support page is available at /zh/support and /fr/support with translated FAQ and contact section"
    - "Blog listing is available at /zh/blog and /fr/blog with translated UI chrome"
    - "Legal pages (privacy, terms) show disclaimer banner at top for zh and fr but NOT for en"
    - "Chinese pages load Noto Sans SC font, French pages do not"
  artifacts:
    - path: "src/pages/zh/privacy.astro"
      provides: "Chinese privacy page"
      contains: "usePrivacyTranslations"
    - path: "src/pages/fr/privacy.astro"
      provides: "French privacy page"
      contains: "usePrivacyTranslations"
    - path: "src/pages/zh/terms.astro"
      provides: "Chinese terms page"
      contains: "useTermsTranslations"
    - path: "src/pages/fr/terms.astro"
      provides: "French terms page"
      contains: "useTermsTranslations"
    - path: "src/pages/zh/support.astro"
      provides: "Chinese support page"
      contains: "useSupportTranslations"
    - path: "src/pages/fr/support.astro"
      provides: "French support page"
      contains: "useSupportTranslations"
    - path: "src/pages/zh/blog/index.astro"
      provides: "Chinese blog listing"
      contains: "useBlogTranslations"
    - path: "src/pages/fr/blog/index.astro"
      provides: "French blog listing"
      contains: "useBlogTranslations"
  key_links:
    - from: "src/pages/zh/privacy.astro"
      to: "src/i18n/utils.ts"
      via: "usePrivacyTranslations('zh')"
      pattern: "usePrivacyTranslations.*zh"
    - from: "src/pages/zh/privacy.astro"
      to: "src/layouts/LegalPageLayout.astro"
      via: "locale='zh' and disclaimerText props"
      pattern: "locale=\"zh\""
    - from: "src/pages/zh/support.astro"
      to: "src/components/FaqAccordion.astro"
      via: "locale='zh' prop"
      pattern: "FaqAccordion.*locale"
---

<objective>
Create 8 locale wrapper pages (zh/fr for privacy, terms, support, blog) that render fully translated content using the namespace files and locale-aware components from Plan 01.

Purpose: Delivers the user-facing translated pages at /zh/ and /fr/ URLs for all legal, support, and blog routes.
Output: 8 new Astro wrapper pages
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-legal-support-blog/21-CONTEXT.md
@.planning/phases/21-legal-support-blog/21-PATTERNS.md
@.planning/phases/21-legal-support-blog/21-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (namespace files and modified components) -->

From src/i18n/utils.ts (new exports from Plan 01):
```typescript
export function usePrivacyTranslations(locale: Locale | undefined): Privacy;
export function useTermsTranslations(locale: Locale | undefined): Terms;
export function useSupportTranslations(locale: Locale | undefined): Support;
export function useBlogTranslations(locale: Locale | undefined): Blog;
```

From src/layouts/LegalPageLayout.astro (modified in Plan 01):
```typescript
interface Props {
  title: string;
  lastUpdated?: string;
  locale?: string;
  disclaimerText?: string;
}
```

From src/components/FaqAccordion.astro (modified in Plan 01):
```typescript
interface Props {
  locale?: string;
}
```

Wrapper page pattern from src/pages/zh/features/recovery-scoring.astro:
```astro
---
import '@fontsource/noto-sans-sc/400.css';
import '@fontsource/noto-sans-sc/700.css';
import { useRecoveryScoringTranslations } from '../../../i18n/utils';
import FeaturePageLayout from '../../../layouts/FeaturePageLayout.astro';
const t = useRecoveryScoringTranslations('zh');
---
<style is:global>
  :root { --font-sans: 'Noto Sans SC', 'General Sans Variable', system-ui, sans-serif; }
</style>
<FeaturePageLayout title={t.meta.title} locale="zh">
  ...
</FeaturePageLayout>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create zh/fr wrapper pages for Privacy and Terms</name>
  <files>
    src/pages/zh/privacy.astro,
    src/pages/fr/privacy.astro,
    src/pages/zh/terms.astro,
    src/pages/fr/terms.astro
  </files>
  <read_first>
    src/pages/privacy.astro,
    src/pages/terms.astro,
    src/pages/zh/features/recovery-scoring.astro,
    src/layouts/LegalPageLayout.astro,
    src/i18n/locales/en/privacy.ts,
    src/i18n/locales/en/terms.ts
  </read_first>
  <action>
Create 4 wrapper pages following the thin wrapper pattern from Phase 20.

**src/pages/zh/privacy.astro:**
```astro
---
import '@fontsource/noto-sans-sc/400.css';
import '@fontsource/noto-sans-sc/700.css';
import { usePrivacyTranslations } from '../../i18n/utils';
import LegalPageLayout from '../../layouts/LegalPageLayout.astro';

const t = usePrivacyTranslations('zh');
---

<style is:global>
  :root { --font-sans: 'Noto Sans SC', 'General Sans Variable', system-ui, sans-serif; }
</style>

<LegalPageLayout
  title={t.meta.title}
  lastUpdated={t.meta.lastUpdated}
  locale="zh"
  disclaimerText={t.disclaimer.text}
>
  <!-- Render ALL translated privacy content from t.* keys as HTML -->
  <!-- Mirror the exact HTML structure of src/pages/privacy.astro -->
  <!-- Every <h2>, <h3>, <p>, <ul>, <li>, <strong>, <a> from the EN page -->
  <!-- must appear here with translated text from t.* -->
</LegalPageLayout>
```

The slot content MUST reproduce the full HTML structure from `src/pages/privacy.astro` (lines 5-81) but with every text string replaced by the corresponding `t.*` key. Include all sections: What Data We Collect, How Data Is Stored, Coach-Athlete Data Sharing, Third-Party Services, Data Retention and Deletion, Your Rights, Children, Changes to This Policy, Contact. Links (mailto, external privacy policy URLs) remain unchanged.

**src/pages/fr/privacy.astro:** Same structure but NO Noto Sans SC imports, NO `<style is:global>` block. Use `usePrivacyTranslations('fr')` and `locale="fr"`.

**src/pages/zh/terms.astro:** Same pattern as zh/privacy but with `useTermsTranslations('zh')`. Slot content reproduces the full HTML structure from `src/pages/terms.astro` (lines 5-61) with all 12 numbered sections translated via `t.*` keys.

**src/pages/fr/terms.astro:** Same as zh/terms but no CJK font, `useTermsTranslations('fr')`, `locale="fr"`.

**Path depth:** These files are at `src/pages/zh/privacy.astro` -- imports use `../../i18n/utils` (two levels up), NOT three levels.

**Critical per D-03:** The `disclaimerText` prop MUST be passed to LegalPageLayout in all 4 wrapper pages. The disclaimer banner renders because `locale !== 'en'`.
  </action>
  <acceptance_criteria>
    - `grep "usePrivacyTranslations" src/pages/zh/privacy.astro` returns a match
    - `grep "usePrivacyTranslations" src/pages/fr/privacy.astro` returns a match
    - `grep "useTermsTranslations" src/pages/zh/terms.astro` returns a match
    - `grep "useTermsTranslations" src/pages/fr/terms.astro` returns a match
    - `grep "locale=\"zh\"" src/pages/zh/privacy.astro` returns a match
    - `grep "locale=\"fr\"" src/pages/fr/privacy.astro` returns a match
    - `grep "disclaimerText" src/pages/zh/privacy.astro` returns a match
    - `grep "disclaimerText" src/pages/fr/terms.astro` returns a match
    - `grep "noto-sans-sc" src/pages/zh/privacy.astro` returns a match (CJK font)
    - `grep "noto-sans-sc" src/pages/fr/privacy.astro` returns NO match (no CJK for French)
    - `npm run build 2>&1 | tail -5` shows successful build
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Privacy and Terms pages build at /zh/privacy, /fr/privacy, /zh/terms, /fr/terms with full translated content and disclaimer banners</done>
</task>

<task type="auto">
  <name>Task 2: Create zh/fr wrapper pages for Support and Blog</name>
  <files>
    src/pages/zh/support.astro,
    src/pages/fr/support.astro,
    src/pages/zh/blog/index.astro,
    src/pages/fr/blog/index.astro
  </files>
  <read_first>
    src/pages/support.astro,
    src/pages/blog/index.astro,
    src/pages/zh/features/recovery-scoring.astro,
    src/i18n/locales/en/support.ts,
    src/i18n/locales/en/blog.ts,
    src/components/FaqAccordion.astro,
    src/layouts/BaseLayout.astro
  </read_first>
  <action>
Create 4 wrapper pages.

**src/pages/zh/support.astro** (per D-05, D-06):
```astro
---
import '@fontsource/noto-sans-sc/400.css';
import '@fontsource/noto-sans-sc/700.css';
import { useSupportTranslations } from '../../i18n/utils';
import LegalPageLayout from '../../layouts/LegalPageLayout.astro';
import FaqAccordion from '../../components/FaqAccordion.astro';

const t = useSupportTranslations('zh');
---

<style is:global>
  :root { --font-sans: 'Noto Sans SC', 'General Sans Variable', system-ui, sans-serif; }
</style>

<LegalPageLayout title={t.meta.title} locale="zh">
  <FaqAccordion locale="zh" />

  <section style="margin-top: var(--space-2xl);">
    <h2 style="font-size: var(--text-heading); font-weight: var(--weight-heading); line-height: var(--leading-heading); letter-spacing: var(--tracking-heading); color: var(--color-text-1);">
      {t.contact.heading}
    </h2>
    <p style="margin-top: var(--space-md); color: var(--color-text-2); line-height: var(--leading-body);">
      {t.contact.subtext}
    </p>
    <p style="margin-top: var(--space-md);">
      <a href="mailto:hanwenma09@gmail.com" style="display: inline-flex; align-items: center; gap: var(--space-sm); padding: var(--space-sm) var(--space-md); background-color: var(--color-accent); color: var(--color-accent-fg); border-radius: var(--radius-md); text-decoration: none; font-weight: var(--weight-label); min-height: 44px;">
        {t.contact.buttonLabel}
      </a>
    </p>
    <p style="margin-top: var(--space-sm); font-size: var(--text-label); color: var(--color-text-3);">
      {t.contact.responseTime}
    </p>
  </section>
</LegalPageLayout>
```

Note: Support page does NOT pass `disclaimerText` to LegalPageLayout (no legal disclaimer needed on support page -- only privacy and terms per D-03). Support page does NOT have `lastUpdated`.

**src/pages/fr/support.astro:** Same structure, no CJK font imports/style, `useSupportTranslations('fr')`, `locale="fr"`.

**src/pages/zh/blog/index.astro** (per D-07, D-08):
```astro
---
import '@fontsource/noto-sans-sc/400.css';
import '@fontsource/noto-sans-sc/700.css';
import { getCollection } from 'astro:content';
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { useBlogTranslations } from '../../../i18n/utils';

const t = useBlogTranslations('zh');
const posts = (await getCollection('blog', ({ data }) =>
  !import.meta.env.PROD || !data.draft
)).sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
---

<style is:global>
  :root { --font-sans: 'Noto Sans SC', 'General Sans Variable', system-ui, sans-serif; }
</style>

<BaseLayout title={t.meta.title} description={t.meta.description} locale="zh">
  <div class="section-spaced">
    <div class="mx-auto px-6" style="max-width: 680px;">
      <h1 style="font-size: var(--text-heading); font-weight: var(--weight-heading); line-height: var(--leading-heading); letter-spacing: var(--tracking-heading); color: var(--color-text-1);">
        {t.page.heading}
      </h1>
      {posts.length === 0 ? (
        <p style="color: var(--color-text-2); margin-top: var(--space-2xl);">
          {t.page.emptyState}
        </p>
      ) : (
        <ul style="list-style: none; padding: 0; margin-top: var(--space-2xl);">
          {posts.map(post => (
            <li style="padding: var(--space-sm) 0;">
              <a href={`/zh/blog/${post.id}`} class="blog-listing-item" style="padding: var(--space-md);">
                <time datetime={post.data.date.toISOString()} style="font-size: var(--text-label); color: var(--color-text-3);">
                  {post.data.date.toLocaleDateString('zh-CN', { year: 'numeric', month: 'long', day: 'numeric' })}
                </time>
                <h2 style="font-size: var(--text-body); font-weight: var(--weight-heading); color: var(--color-text-1); margin-top: var(--space-xs);">
                  {post.data.title}
                </h2>
                <p style="font-size: var(--text-body); color: var(--color-text-2); margin-top: var(--space-xs);">
                  {post.data.description}
                </p>
              </a>
            </li>
          ))}
        </ul>
      )}
    </div>
  </div>
</BaseLayout>
```

Note the blog path depth: `src/pages/zh/blog/index.astro` is 3 levels deep, so imports use `../../../layouts/BaseLayout.astro` and `../../../i18n/utils`. Blog post links use `/zh/blog/${post.id}` for locale-aware routing. Date formatting uses `'zh-CN'` locale.

**src/pages/fr/blog/index.astro:** Same structure, no CJK font, `useBlogTranslations('fr')`, `locale="fr"`. Blog post links use `/fr/blog/${post.id}`. Date formatting uses `'fr-FR'` locale.

**Directory creation:** `src/pages/zh/blog/` and `src/pages/fr/blog/` directories may not exist yet. Create them before writing the files.
  </action>
  <acceptance_criteria>
    - `grep "useSupportTranslations" src/pages/zh/support.astro` returns a match
    - `grep "useSupportTranslations" src/pages/fr/support.astro` returns a match
    - `grep "FaqAccordion" src/pages/zh/support.astro` returns a match
    - `grep 'locale="zh"' src/pages/zh/support.astro` returns a match
    - `grep "useBlogTranslations" src/pages/zh/blog/index.astro` returns a match
    - `grep "useBlogTranslations" src/pages/fr/blog/index.astro` returns a match
    - `grep "zh-CN" src/pages/zh/blog/index.astro` returns a match (Chinese date format)
    - `grep "fr-FR" src/pages/fr/blog/index.astro` returns a match (French date format)
    - `grep "noto-sans-sc" src/pages/zh/support.astro` returns a match
    - `grep "noto-sans-sc" src/pages/fr/support.astro` returns NO match
    - `npm run build 2>&1 | tail -5` shows successful build
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Support and Blog pages build at /zh/support, /fr/support, /zh/blog, /fr/blog with fully translated content, correct CJK font handling, and locale-appropriate date formatting</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static build | All content is statically generated at build time, no runtime user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Spoofing | Locale wrapper pages | accept | Static pages, no auth context, locale is build-time constant |
| T-21-04 | Information Disclosure | Blog post listing | accept | Blog posts are public content by design |
</threat_model>

<verification>
- `npm run build` succeeds with all 8 new pages generating
- All 8 new routes resolve: /zh/privacy, /fr/privacy, /zh/terms, /fr/terms, /zh/support, /fr/support, /zh/blog, /fr/blog
- EN pages at /privacy, /terms, /support, /blog remain unchanged
- Disclaimer banner appears on zh/fr privacy and terms pages, NOT on support or blog
</verification>

<success_criteria>
- 8 wrapper pages created and building successfully
- Privacy and Terms pages show disclaimer banner for non-EN locales (per D-03)
- Support pages render translated FAQ accordion and contact section (per D-05, D-06)
- Blog listing pages show translated UI chrome with empty state (per D-07)
- Chinese pages load Noto Sans SC, French pages do not
- All locale-aware routing works (blog links to /zh/blog/*, date formatting per locale)
</success_criteria>

<output>
After completion, create `.planning/phases/21-legal-support-blog/21-02-SUMMARY.md`
</output>

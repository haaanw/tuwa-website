---
phase: 20-feature-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/FeaturePageLayout.astro
  - src/layouts/CoachingPageLayout.astro
  - src/components/FeatureCTA.astro
  - src/i18n/locales/en/common.ts
  - src/i18n/locales/zh/common.ts
  - src/i18n/locales/fr/common.ts
autonomous: true
requirements: [I18N-07, I18N-08]

must_haves:
  truths:
    - "FeaturePageLayout accepts a locale prop and passes it to BaseLayout and FeatureCTA"
    - "CoachingPageLayout accepts a locale prop and passes it to BaseLayout and FeatureCTA"
    - "FeatureCTA renders translated headline, body, badge alt, and aria-label from common.ts"
    - "Existing English feature pages continue to work unchanged (locale defaults to 'en')"
  artifacts:
    - path: "src/layouts/FeaturePageLayout.astro"
      provides: "locale prop threading to BaseLayout and FeatureCTA"
      contains: "locale"
    - path: "src/layouts/CoachingPageLayout.astro"
      provides: "locale prop threading to BaseLayout and FeatureCTA"
      contains: "locale"
    - path: "src/components/FeatureCTA.astro"
      provides: "locale-aware CTA with useTranslations"
      contains: "useTranslations"
    - path: "src/i18n/locales/en/common.ts"
      provides: "featureCTA translation keys"
      contains: "featureCTA"
    - path: "src/i18n/locales/zh/common.ts"
      provides: "Chinese featureCTA translation keys"
      contains: "featureCTA"
    - path: "src/i18n/locales/fr/common.ts"
      provides: "French featureCTA translation keys"
      contains: "featureCTA"
  key_links:
    - from: "src/layouts/FeaturePageLayout.astro"
      to: "src/components/FeatureCTA.astro"
      via: "locale prop forwarding"
      pattern: "FeatureCTA locale="
    - from: "src/components/FeatureCTA.astro"
      to: "src/i18n/locales/en/common.ts"
      via: "useTranslations"
      pattern: "t\\.featureCTA\\."
---

<objective>
Wire locale support into feature page layouts and FeatureCTA component so that zh/fr wrapper pages (created in later plans) render translated CTA sections.

Purpose: Without this wiring, all zh/fr feature pages would display English "Start training with confidence" CTA footers regardless of locale. This plan establishes the locale prop chain: wrapper page -> layout -> BaseLayout + FeatureCTA.

Output: Two modified layouts with locale prop, one locale-aware FeatureCTA component, three updated common.ts files with featureCTA keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-feature-pages/20-CONTEXT.md
@.planning/phases/20-feature-pages/20-RESEARCH.md
@.planning/phases/20-feature-pages/20-PATTERNS.md

<interfaces>
<!-- From src/layouts/CJKLayout.astro — locale prop threading pattern to replicate -->
From src/layouts/CJKLayout.astro:
```typescript
interface Props {
  title: string;
  description: string;
  ogImage?: string;
  canonical?: string;
  type?: string;
}
const props = Astro.props;
// <BaseLayout {...props} locale="zh">
```

From src/i18n/utils.ts:
```typescript
export type Locale = 'en' | 'zh' | 'fr';
export function useTranslations(locale: Locale | undefined): Common { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add featureCTA keys to common.ts in all 3 locales</name>
  <files>src/i18n/locales/en/common.ts, src/i18n/locales/zh/common.ts, src/i18n/locales/fr/common.ts</files>
  <read_first>
    - src/i18n/locales/en/common.ts (current structure, find insertion point before closing `} as const;`)
    - src/i18n/locales/zh/common.ts (current structure, find insertion point before closing brace)
    - src/i18n/locales/fr/common.ts (current structure, find insertion point before closing brace)
    - src/components/FeatureCTA.astro (source of hardcoded English strings to extract)
  </read_first>
  <action>
Add a `featureCTA` object to common.ts in all 3 locales. Insert before the closing `} as const;` (en) or closing `};` (zh/fr).

**en/common.ts** — add this object (per D-04, CTA keys go in common.ts shared across all feature pages):
```typescript
  featureCTA: {
    headline: 'Start training with confidence',
    body: 'Download Tuwa and take the guesswork out of recovery and load management.',
    badgeAlt: 'Download on the App Store',
    badgeAriaLabel: 'Download Tuwa on the App Store',
  },
```

**zh/common.ts** — add:
```typescript
  featureCTA: {
    headline: '开启自信的训练之旅',
    body: '下载 Tuwa，让恢复与负荷管理不再靠猜测。',
    badgeAlt: '在 App Store 下载',
    badgeAriaLabel: '在 App Store 下载 Tuwa',
  },
```

**fr/common.ts** — add:
```typescript
  featureCTA: {
    headline: "Commence a t'entrainer avec confiance",
    body: "Telecharge Tuwa et dis adieu aux approximations en matiere de recuperation et de charge.",
    badgeAlt: "Telecharger sur l'App Store",
    badgeAriaLabel: "Telecharger Tuwa sur l'App Store",
  },
```
Note: Use proper French accented characters (e with accent aigu, a with accent grave, i with trema) — the above is ASCII-simplified for the plan. The actual values should be: `Commence a t'entrainer` -> `Commence \u00e0 t'entra\u00eener`, etc. Match the accent patterns already used in fr/common.ts and fr/home.ts.
  </action>
  <verify>
    <automated>grep -c "featureCTA" src/i18n/locales/en/common.ts src/i18n/locales/zh/common.ts src/i18n/locales/fr/common.ts && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/i18n/locales/en/common.ts contains `featureCTA: {` with keys: headline, body, badgeAlt, badgeAriaLabel
    - src/i18n/locales/zh/common.ts contains `featureCTA: {` with Chinese translations
    - src/i18n/locales/fr/common.ts contains `featureCTA: {` with French translations using "tu" register
    - `npx tsc --noEmit` passes (zh/fr types match en common type)
  </acceptance_criteria>
  <done>All 3 common.ts files contain featureCTA keys, TypeScript compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Wire locale prop into FeaturePageLayout, CoachingPageLayout, and FeatureCTA</name>
  <files>src/layouts/FeaturePageLayout.astro, src/layouts/CoachingPageLayout.astro, src/components/FeatureCTA.astro</files>
  <read_first>
    - src/layouts/FeaturePageLayout.astro (current Props interface, BaseLayout call, FeatureCTA call)
    - src/layouts/CoachingPageLayout.astro (current Props interface, BaseLayout call, FeatureCTA call)
    - src/components/FeatureCTA.astro (current frontmatter, hardcoded strings at lines 23, 33, 45, 49)
    - src/i18n/utils.ts (useTranslations function signature)
    - src/i18n/locales/en/common.ts (verify featureCTA keys exist from Task 1)
  </read_first>
  <action>
**FeaturePageLayout.astro:**
1. Add `locale?: string;` to the Props interface (after `hookLine` line)
2. Add `locale = 'en'` to the destructuring assignment (e.g., `const { title, description, ..., locale = 'en' } = Astro.props;`)
3. Change the BaseLayout call to include locale: `<BaseLayout title={title} description={description} ogImage={ogImage} locale={locale}>`
4. Change the FeatureCTA call to include locale: `<FeatureCTA locale={locale} />`

**CoachingPageLayout.astro:**
Same 4 changes as FeaturePageLayout — add `locale?: string;` to Props, destructure with default `'en'`, pass to BaseLayout, pass to FeatureCTA.

**FeatureCTA.astro:**
1. Add import at top of frontmatter: `import { useTranslations } from '../i18n/utils';`
2. Add Props interface: `interface Props { locale?: string; }`
3. Add destructure: `const { locale = 'en' } = Astro.props;`
4. Add translations call: `const t = useTranslations(locale as any);`
5. Replace hardcoded strings with translation keys:
   - Line with `Start training with confidence` -> `{t.featureCTA.headline}`
   - Line with `Download Tuwa and take the guesswork out of recovery and load management.` -> `{t.featureCTA.body}`
   - Line with `aria-label="Download Tuwa on the App Store"` -> `aria-label={t.featureCTA.badgeAriaLabel}`
   - Line with `alt="Download on the App Store"` -> `alt={t.featureCTA.badgeAlt}`

IMPORTANT: Keep `import { APP_STORE_URL } from '../config';` — only add the new import, do not remove existing ones. The APP_STORE_URL is still used for the badge link href.
  </action>
  <verify>
    <automated>grep -n "locale" src/layouts/FeaturePageLayout.astro src/layouts/CoachingPageLayout.astro && grep -n "useTranslations\|featureCTA" src/components/FeatureCTA.astro && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/layouts/FeaturePageLayout.astro contains `locale?: string` in Props interface
    - src/layouts/FeaturePageLayout.astro contains `locale={locale}` passed to BaseLayout
    - src/layouts/FeaturePageLayout.astro contains `<FeatureCTA locale={locale}`
    - src/layouts/CoachingPageLayout.astro contains `locale?: string` in Props interface
    - src/layouts/CoachingPageLayout.astro contains `locale={locale}` passed to BaseLayout
    - src/layouts/CoachingPageLayout.astro contains `<FeatureCTA locale={locale}`
    - src/components/FeatureCTA.astro contains `import { useTranslations }`
    - src/components/FeatureCTA.astro contains `t.featureCTA.headline`
    - src/components/FeatureCTA.astro does NOT contain hardcoded `Start training with confidence`
    - `npm run build` succeeds (existing English feature pages still render correctly)
  </acceptance_criteria>
  <done>Both layouts thread locale prop to BaseLayout and FeatureCTA; FeatureCTA reads all strings from common.ts translations; existing English pages build and render unchanged</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Translation files are static TypeScript constants compiled at build time. No user input, no runtime data flow.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | T (Tampering) | Translation files | accept | Static .ts files in version control; no runtime modification possible |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` succeeds with same page count as before
- Existing English feature pages at /features/* render identically (no visual regression)
</verification>

<success_criteria>
- FeaturePageLayout and CoachingPageLayout accept locale prop and forward to BaseLayout + FeatureCTA
- FeatureCTA renders translated strings from common.ts featureCTA keys
- All 3 common.ts locales have featureCTA with headline, body, badgeAlt, badgeAriaLabel
- Build succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/20-feature-pages/20-01-SUMMARY.md`
</output>

---
phase: 04-blog-polish
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - public/robots.txt
  - public/og-default.png
autonomous: false
requirements: [PERF-01, PERF-02, PERF-03, PERF-04, PERF-05]
must_haves:
  truths:
    - "robots.txt is accessible at /robots.txt and references sitemap-index.xml"
    - "Default OG image is a branded 1200x630 PNG replacing the 1x1 placeholder"
    - "XML sitemap is generated at build time at /sitemap-index.xml"
    - "All pages have proper heading hierarchy (h1 -> h2 -> h3, no skips)"
    - "All images have alt text or are decorative (aria-hidden)"
    - "Canonical URLs on all pages resolve to https://tuwa.app/..."
    - "Lighthouse performance score >= 95 on landing page"
  artifacts:
    - path: "public/robots.txt"
      provides: "Crawler directives with sitemap reference"
      contains: "sitemap-index.xml"
    - path: "public/og-default.png"
      provides: "Branded 1200x630 default OG image"
      min_lines: 0
  key_links:
    - from: "public/robots.txt"
      to: "dist/sitemap-index.xml"
      via: "Sitemap URL reference"
      pattern: "sitemap-index.xml"
    - from: "src/components/SEO.astro"
      to: "public/og-default.png"
      via: "ogImage default prop value"
      pattern: "og-default.png"
---

<objective>
Create robots.txt, generate a branded default OG image, audit all pages for semantic HTML compliance, verify canonical URLs, and ensure Lighthouse performance >= 95.

Purpose: Close out all PERF requirements -- the site must be crawlable, semantically correct, and performant before v1 launch.
Output: robots.txt, branded OG image, semantic HTML fixes across existing pages, Lighthouse verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-blog-polish/04-CONTEXT.md
@.planning/phases/04-blog-polish/04-RESEARCH.md
@.planning/phases/04-blog-polish/04-PATTERNS.md
@.planning/phases/04-blog-polish/04-01-SUMMARY.md
@.planning/phases/04-blog-polish/04-02-SUMMARY.md

<interfaces>
<!-- SEO.astro default ogImage value -->
From src/components/SEO.astro:
```astro
ogImage = "/og-default.png",
```

<!-- Sitemap integration already wired -->
From astro.config.mjs:
```javascript
integrations: [mdx(), sitemap()],
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create robots.txt, generate branded OG image, audit and fix semantic HTML across all pages</name>
  <files>public/robots.txt, public/og-default.png</files>
  <read_first>
    src/components/SEO.astro
    src/layouts/BaseLayout.astro
    src/components/Header.astro
    src/components/Footer.astro
    src/pages/index.astro
    src/pages/privacy.astro
    src/pages/terms.astro
    src/pages/support.astro
    src/pages/features/recovery-scoring.astro
    src/pages/features/coaching.astro
    src/layouts/LegalPageLayout.astro
    src/layouts/BlogPostLayout.astro
  </read_first>
  <action>
1. Create `public/robots.txt` with this exact content (per D-10):
```
User-agent: *
Allow: /

Sitemap: https://tuwa.app/sitemap-index.xml
```
CRITICAL: Reference `sitemap-index.xml` NOT `sitemap.xml` -- @astrojs/sitemap generates `sitemap-index.xml` as the root file.

2. Generate a branded default OG image (1200x630 PNG) to replace the 1x1 placeholder at `public/og-default.png` (per D-09):
   - Use a Node.js script with the `sharp` library (already available as Astro dependency) to generate the image
   - Design: solid background using the travertine color (#F4F1ED), centered text "Tuwa" in dark text (#1C1915) at ~72px, subtitle "Precision Training Load & Recovery" in secondary color (#696560) at ~24px
   - Output dimensions: exactly 1200x630 pixels
   - Save to `public/og-default.png`
   - Delete the generation script after creating the image (per Phase 3 pattern -- OG images generated via temp script then deleted)
   - Verify the file is larger than 1KB (the current placeholder is 70 bytes)

Example generation script (create temporarily, run, then delete):
```javascript
// temp-og-gen.mjs
import sharp from 'sharp';

const width = 1200;
const height = 630;

const svg = `
<svg width="${width}" height="${height}" xmlns="http://www.w3.org/2000/svg">
  <rect width="100%" height="100%" fill="#F4F1ED"/>
  <text x="50%" y="45%" text-anchor="middle" font-family="system-ui, sans-serif" font-size="72" font-weight="600" fill="#1C1915">Tuwa</text>
  <text x="50%" y="58%" text-anchor="middle" font-family="system-ui, sans-serif" font-size="24" fill="#696560">Precision Training Load &amp; Recovery</text>
</svg>`;

await sharp(Buffer.from(svg)).png().toFile('public/og-default.png');
console.log('OG image generated');
```
Run: `node temp-og-gen.mjs` then `rm temp-og-gen.mjs`

3. Semantic HTML audit across ALL pages and layouts (per D-12):
   Read every page and layout file listed in read_first. For each, verify:
   - Proper heading hierarchy: each page has exactly one h1, h2s follow h1, h3s follow h2 (no skips like h1->h3)
   - Landmark roles: `<main>`, `<nav>`, `<header>`, `<footer>` used correctly (BaseLayout already has `<main>` wrapping slot)
   - All `<img>` elements have `alt` attribute (or `aria-hidden="true"` if decorative)
   - All `<a>` elements with external URLs have descriptive text (not bare URLs)

   Fix any issues found. Common issues to check:
   - Feature pages: verify h1 in hero, h2 for sections, h3 for subsections
   - Legal pages: verify heading hierarchy in prose content
   - Index page: verify h1 exists in hero section
   - Header: verify nav has `aria-label` (it already does: "Main navigation")
   - Images in feature pages: verify all `<img>` and Astro `<Image>` have alt text

4. Canonical URL verification (per D-13, PERF-05):
   After `npm run build`, check 3-5 generated HTML files in `dist/` to confirm:
   - `<link rel="canonical" href="https://tuwa.app/...">` (NOT localhost)
   - Each page's canonical matches its actual URL path

5. Sitemap verification (per PERF-03, D-11):
   After `npm run build`, verify:
   - `dist/sitemap-index.xml` exists
   - `dist/sitemap-0.xml` exists and lists all public pages
   - Blog pages are NOT in sitemap (no blog posts exist yet, so nothing to list)

6. Run `npm run build` and verify all output is correct.
  </action>
  <verify>
    <automated>test -f public/robots.txt && grep -q "sitemap-index.xml" public/robots.txt && test -f public/og-default.png && test $(stat -f%z public/og-default.png 2>/dev/null || stat -c%s public/og-default.png 2>/dev/null) -gt 1000 && npm run build 2>&1 | tail -5 && test -f dist/robots.txt && test -f dist/sitemap-index.xml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - public/robots.txt contains `User-agent: *`
    - public/robots.txt contains `Sitemap: https://tuwa.app/sitemap-index.xml`
    - public/robots.txt does NOT contain `sitemap.xml` (only `sitemap-index.xml`)
    - public/og-default.png file size is > 1000 bytes (not the 70-byte placeholder)
    - `npm run build` completes without errors
    - dist/robots.txt exists (copied from public/)
    - dist/sitemap-index.xml exists
    - dist/sitemap-0.xml exists
    - No temp OG generation script remains in repo
    - Every page file has exactly one h1 element
    - All img elements in page files have alt attributes
  </acceptance_criteria>
  <done>robots.txt created with correct sitemap reference, branded OG image replaces placeholder, sitemap generated at build, semantic HTML verified across all pages</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Lighthouse score, font rendering, blog page, and OG image</name>
  <files>none</files>
  <action>
    Human verification checkpoint. The executor presents the dev server and build output to the user for visual and performance verification. No code changes in this task -- purely verification of all Phase 4 deliverables across Plans 01, 02, and 03.
  </action>
  <what-built>
    Complete Phase 4 deliverables: General Sans font across the site, blog listing page at /blog, branded OG image, robots.txt, sitemap, semantic HTML fixes, and all performance optimizations.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Visit http://localhost:4321 -- verify General Sans font renders (not Alpino). Text should appear slightly different from before.
    3. Visit http://localhost:4321/blog -- verify the blog listing page shows "Blog" heading and "Posts coming soon." empty state.
    4. Visit http://localhost:4321/robots.txt -- verify it shows the robots directives with sitemap reference.
    5. Open browser DevTools Network tab, reload the page, search for font requests -- confirm "General Sans" appears in font URLs (not "Alpino").
    6. Run a production build: `npm run build`
    7. Check dist/sitemap-index.xml exists and contains sitemap references.
    8. Open public/og-default.png -- verify it is a branded 1200x630 image with "Tuwa" text (not a 1x1 pixel).
    9. Run Lighthouse on the landing page (Chrome DevTools > Lighthouse > Performance, both mobile and desktop). Target: >= 95 on both.
    10. If Lighthouse score < 95, note the specific issues for fixing.
  </how-to-verify>
  <verify>Human confirms all visual and performance checks pass</verify>
  <done>User has approved font rendering, blog page layout, OG image quality, and Lighthouse score >= 95</done>
  <resume-signal>Type "approved" if all checks pass. If Lighthouse < 95, describe the specific bottlenecks shown in the report.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static assets | robots.txt and OG image are static files with no user input |
| Crawler access | robots.txt allows all crawlers -- intentional for a public marketing site |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | robots.txt | accept | Public marketing site -- all pages intended to be crawled. No sensitive routes to block. |
| T-04-06 | Information Disclosure | sitemap.xml | accept | Sitemap lists all public pages -- this is the intended behavior for SEO. Draft posts excluded by build-time filter. |
</threat_model>

<verification>
- robots.txt accessible at /robots.txt after build
- sitemap-index.xml and sitemap-0.xml exist in dist/
- OG image is 1200x630 branded PNG (not 1x1 placeholder)
- All pages pass heading hierarchy check (one h1, proper nesting)
- Canonical URLs point to https://tuwa.app/... (not localhost)
- Lighthouse performance >= 95 on landing page
</verification>

<success_criteria>
- robots.txt allows all crawlers and references correct sitemap URL
- XML sitemap generated at build time with all public pages
- Branded default OG image replaces 1x1 placeholder
- All pages have proper semantic HTML (headings, landmarks, alt text)
- Canonical URLs correct on all pages
- Lighthouse performance >= 95 on landing page (mobile and desktop)
- Human verification confirms font rendering and visual quality
</success_criteria>

<output>
After completion, create `.planning/phases/04-blog-polish/04-03-SUMMARY.md`
</output>

---
phase: 03-content-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/components/ScreenshotBlock.astro
  - src/components/FeatureCTA.astro
  - src/layouts/FeaturePageLayout.astro
  - src/assets/screenshots/recovery.png
  - src/assets/screenshots/workload.png
  - src/assets/screenshots/active-workout.png
  - public/og/recovery-scoring.png
  - public/og/workload-tracking.png
  - public/og/smart-templates.png
  - public/og/cold-start.png
  - public/og/coaching.png
  - package.json
  - package-lock.json
autonomous: true
requirements: [FEAT-06, FEAT-07]

must_haves:
  truths:
    - "FeaturePageLayout wraps BaseLayout and renders hero, screenshot, slot content, and CTA sections"
    - "ScreenshotBlock renders an Astro Image when src is provided and a placeholder div when src is omitted"
    - "FeatureCTA displays App Store badge using APP_STORE_URL from config.ts and includes the IntersectionObserver script"
    - "Typography prose classes are activated via @plugin directive in global.css"
    - "Screenshot assets (recovery, workload, active-workout) are available in src/assets/screenshots/"
    - "chart.js is installed and importable"
    - "5 unique static OG images exist in public/og/ for each feature page"
  artifacts:
    - path: "src/layouts/FeaturePageLayout.astro"
      provides: "Reusable layout for 4 standard feature pages"
      exports: ["default"]
    - path: "src/components/ScreenshotBlock.astro"
      provides: "Screenshot display with placeholder fallback"
      exports: ["default"]
    - path: "src/components/FeatureCTA.astro"
      provides: "Feature page CTA section with IntersectionObserver"
      exports: ["default"]
    - path: "src/styles/global.css"
      provides: "Typography plugin activation"
      contains: "@plugin"
    - path: "public/og/recovery-scoring.png"
      provides: "OG image for Recovery Scoring page"
    - path: "public/og/workload-tracking.png"
      provides: "OG image for Workload Tracking page"
    - path: "public/og/smart-templates.png"
      provides: "OG image for Smart Templates page"
    - path: "public/og/cold-start.png"
      provides: "OG image for Cold-Start Onboarding page"
    - path: "public/og/coaching.png"
      provides: "OG image for Coaching page"
  key_links:
    - from: "src/layouts/FeaturePageLayout.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "import wrapping"
      pattern: "import BaseLayout from"
    - from: "src/components/FeatureCTA.astro"
      to: "src/config.ts"
      via: "APP_STORE_URL import"
      pattern: "import.*APP_STORE_URL.*from"
    - from: "src/components/ScreenshotBlock.astro"
      to: "astro:assets"
      via: "Image component import"
      pattern: "import.*Image.*from.*astro:assets"
---

<objective>
Create all reusable infrastructure for Phase 3 content pages: install chart.js, activate the typography plugin, copy screenshot assets, generate static per-page OG images, and build the three core components (ScreenshotBlock, FeatureCTA, FeaturePageLayout) that every standard feature page depends on.

Purpose: Unblocks Wave 2 plans that create the actual pages. All reusable layouts, components, and OG assets must exist before page-level content can be written.
Output: 3 new Astro files (1 layout, 2 components), 3 screenshot assets, 5 OG images, chart.js installed, typography plugin activated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-pages/03-CONTEXT.md
@.planning/phases/03-content-pages/03-RESEARCH.md
@.planning/phases/03-content-pages/03-PATTERNS.md
@.planning/phases/03-content-pages/03-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/layouts/BaseLayout.astro:
```astro
interface Props {
  title: string;
  description: string;
  ogImage?: string;
  canonical?: string;
}
```

From src/components/SEO.astro:
```astro
interface Props {
  title: string;
  description: string;
  ogImage?: string;
  canonical?: string;
}
// ogImage must start with "/" for correct resolution (prepends siteOrigin)
// Default ogImage: "/og-default.png"
```

From src/config.ts:
```typescript
export const APP_STORE_URL = 'https://apps.apple.com/app/tuwa';
```

From src/components/LandingCTA.astro (IntersectionObserver script to copy):
```astro
<script is:inline>
  (function () {
    var observer = new IntersectionObserver(
      function (entries) {
        entries.forEach(function (entry) {
          if (entry.isIntersecting) {
            entry.target.classList.add('is-visible');
            observer.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.15 }
    );
    document.querySelectorAll('[data-animate]').forEach(function (el) {
      observer.observe(el);
    });
  })();
</script>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install chart.js, activate typography plugin, copy screenshot assets, create OG images</name>
  <files>package.json, package-lock.json, src/styles/global.css, src/assets/screenshots/recovery.png, src/assets/screenshots/workload.png, src/assets/screenshots/active-workout.png, public/og/recovery-scoring.png, public/og/workload-tracking.png, public/og/smart-templates.png, public/og/cold-start.png, public/og/coaching.png</files>
  <read_first>
    - src/styles/global.css (current state -- add plugin directive after @import "tailwindcss" line)
    - package.json (current dependencies)
    - src/assets/screenshots/ (existing files)
  </read_first>
  <action>
1. Run `npm install chart.js` in the project root. This adds chart.js ^4.5.1 to dependencies.

2. Copy screenshot assets from the Tonus project:
   - `cp ~/Desktop/Tonus/appstore\ screenshots/6\"3/Recovery_framed.png src/assets/screenshots/recovery.png`
   - `cp ~/Desktop/Tonus/appstore\ screenshots/6\"3/Workload_framed.png src/assets/screenshots/workload.png`
   - `cp ~/Desktop/Tonus/appstore\ screenshots/6\"3/ActiveWorkout_framed.png src/assets/screenshots/active-workout.png`

3. Add the typography plugin directive to `src/styles/global.css`. Insert `@plugin "@tailwindcss/typography";` immediately after the `@import "tailwindcss";` line (line 3). The file should read:
   ```css
   @import "tailwindcss";
   @plugin "@tailwindcss/typography";
   ```
   Do NOT modify any other part of global.css.

4. Create 5 static OG images (1200x630 PNG each) in `public/og/` -- one per feature page per FEAT-06. Use `sips` (macOS built-in) to create simple travertine-background images with feature title text. For each image:

   ```bash
   mkdir -p public/og
   ```

   Generate each OG image using ImageMagick (`convert`) or a Node script. The simplest approach: create a small Node script that uses the `canvas` npm package (or `sharp` which is already bundled with Astro) to generate 1200x630 PNGs with:
   - Background color: #F4F1ED (--color-bg, travertine)
   - "Tuwa" wordmark text centered at top in #1C1915 (--color-text-1)
   - Feature name centered below in #2B5240 (--color-accent)
   - Font: system sans-serif is acceptable for OG images (Alpino TTF could be used if available, but OG images don't need exact font match)

   Create a temporary script `scripts/generate-og.mjs`:
   ```javascript
   import sharp from 'sharp';

   const pages = [
     { slug: 'recovery-scoring', title: 'Recovery Scoring' },
     { slug: 'workload-tracking', title: 'Workload Tracking' },
     { slug: 'smart-templates', title: 'Smart Templates' },
     { slug: 'cold-start', title: 'Cold-Start Onboarding' },
     { slug: 'coaching', title: 'Coach + Athlete' },
   ];

   for (const { slug, title } of pages) {
     const svg = `
       <svg width="1200" height="630" xmlns="http://www.w3.org/2000/svg">
         <rect width="1200" height="630" fill="#F4F1ED"/>
         <text x="600" y="260" text-anchor="middle" font-family="sans-serif" font-size="36" font-weight="600" fill="#1C1915">Tuwa</text>
         <text x="600" y="340" text-anchor="middle" font-family="sans-serif" font-size="48" font-weight="600" fill="#2B5240">${title}</text>
         <text x="600" y="420" text-anchor="middle" font-family="sans-serif" font-size="24" fill="#696560">Precision Training Load &amp; Recovery</text>
       </svg>`;
     await sharp(Buffer.from(svg)).png().toFile(`public/og/${slug}.png`);
   }
   ```

   Run: `node scripts/generate-og.mjs`

   After generation, delete the script: `rm scripts/generate-og.mjs && rmdir scripts 2>/dev/null`

   The resulting files:
   - public/og/recovery-scoring.png (1200x630)
   - public/og/workload-tracking.png (1200x630)
   - public/og/smart-templates.png (1200x630)
   - public/og/cold-start.png (1200x630)
   - public/og/coaching.png (1200x630)

5. Verify with `npm run build` -- build must succeed with no errors.
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/tuwa-website && npm run build 2>&1 | tail -5 && npx astro check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "chart.js" in dependencies
    - src/styles/global.css contains `@plugin "@tailwindcss/typography"`
    - src/assets/screenshots/recovery.png exists and is > 100KB
    - src/assets/screenshots/workload.png exists and is > 100KB
    - src/assets/screenshots/active-workout.png exists and is > 100KB
    - public/og/recovery-scoring.png exists and is a valid 1200x630 PNG
    - public/og/workload-tracking.png exists and is a valid 1200x630 PNG
    - public/og/smart-templates.png exists and is a valid 1200x630 PNG
    - public/og/cold-start.png exists and is a valid 1200x630 PNG
    - public/og/coaching.png exists and is a valid 1200x630 PNG
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Chart.js installed, typography plugin active, 3 screenshot assets copied, 5 unique OG images generated, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Create ScreenshotBlock, FeatureCTA, and FeaturePageLayout</name>
  <files>src/components/ScreenshotBlock.astro, src/components/FeatureCTA.astro, src/layouts/FeaturePageLayout.astro</files>
  <read_first>
    - src/layouts/BaseLayout.astro (layout wrapping pattern -- Props interface, slot usage)
    - src/components/LandingCTA.astro (FeatureCTA source -- adapt this, copy IntersectionObserver script verbatim)
    - src/components/Hero.astro (Image import pattern, heading styles)
    - src/config.ts (APP_STORE_URL import path)
    - src/styles/global.css (design token names and values for inline styles)
    - .planning/phases/03-content-pages/03-PATTERNS.md (exact patterns to follow)
    - .planning/phases/03-content-pages/03-UI-SPEC.md (component specs, spacing, colors)
  </read_first>
  <action>
**Create `src/components/ScreenshotBlock.astro`:**

```astro
---
import { Image } from 'astro:assets';

interface Props {
  src?: ImageMetadata;
  alt: string;
  placeholderLabel?: string;
}

const { src, alt, placeholderLabel = 'Screenshot coming soon' } = Astro.props;
---
<div class="mx-auto" style="max-width: 320px;">
  {src ? (
    <Image
      src={src}
      alt={alt}
      width={320}
      quality={90}
      format="webp"
      style="box-shadow: 0 4px 24px rgba(28,25,21,0.10); display: block;"
    />
  ) : (
    <div
      role="img"
      aria-label={alt}
      style="
        background-color: var(--color-surface);
        border-radius: var(--radius-lg);
        aspect-ratio: 9/19.5;
        display: flex;
        align-items: center;
        justify-content: center;
        color: var(--color-text-3);
        font-size: var(--text-label);
        text-align: center;
        padding: var(--space-md);
      "
    >
      {placeholderLabel}
    </div>
  )}
</div>
```

Note: Framed screenshots already have device bezels baked in. Use `box-shadow` only (no `border-radius`) to avoid doubled-frame visual. This matches the Hero.astro treatment of Dashboard_framed.png.

**Create `src/components/FeatureCTA.astro`:**

Adapt from LandingCTA.astro with these changes:
1. Remove QR code import (`import QRCode from 'qrcode'`) and QR generation code
2. Remove the QR code HTML block (the `hidden md:flex` div)
3. Keep the IntersectionObserver `<script is:inline>` IIFE exactly as-is from LandingCTA
4. Keep `import { APP_STORE_URL } from '../config';`
5. Change h2 heading to: "Start training with confidence"
6. Change supporting p to: "Download Tuwa and take the guesswork out of recovery and load management."
7. Keep the App Store badge `<a>` block exactly (same src, alt, aria-label, rel="noopener noreferrer")
8. Keep section wrapper: `data-animate`, `background-color: var(--color-surface)`, `border-top: 1px solid var(--color-divider)`
9. Keep container: `mx-auto max-w-6xl px-6`, padding `var(--space-3xl)` top/bottom
10. Keep inner: `mx-auto text-center`, `max-width: 640px`
11. h2 styles: `font-size: var(--text-heading); font-weight: 600; line-height: var(--leading-heading); letter-spacing: var(--tracking-heading); color: var(--color-text-1);`
12. p styles: `margin-top: var(--space-lg); color: var(--color-text-2);`
13. Badge container: `flex flex-col items-center`, `margin-top: var(--space-xl);`

**Create `src/layouts/FeaturePageLayout.astro`:**

```astro
---
import BaseLayout from './BaseLayout.astro';
import ScreenshotBlock from '../components/ScreenshotBlock.astro';
import FeatureCTA from '../components/FeatureCTA.astro';

interface Props {
  title: string;
  description: string;
  ogImage?: string;
  screenshot?: ImageMetadata;
  screenshotAlt: string;
  outcomeStatement: string;
  hookLine: string;
}

const { title, description, ogImage, screenshot, screenshotAlt, outcomeStatement, hookLine } = Astro.props;
---
<BaseLayout title={title} description={description} ogImage={ogImage}>
  <!-- Hero -->
  <section data-animate style="padding-top: var(--space-3xl); padding-bottom: var(--space-2xl);">
    <div class="mx-auto text-center" style="max-width: 768px; padding: 0 var(--space-md);">
      <h1
        style="
          font-size: var(--text-display);
          font-weight: 600;
          line-height: var(--leading-display);
          letter-spacing: var(--tracking-display);
          color: var(--color-text-1);
        "
      >
        {outcomeStatement}
      </h1>
      <p
        class="mx-auto"
        style="
          margin-top: var(--space-lg);
          max-width: 560px;
          font-size: var(--text-body);
          line-height: var(--leading-body);
          color: var(--color-text-2);
        "
      >
        {hookLine}
      </p>
    </div>
  </section>

  <!-- Screenshot -->
  <section data-animate style="padding-bottom: var(--space-3xl);">
    <ScreenshotBlock src={screenshot} alt={screenshotAlt} placeholderLabel={`${title} Screenshot -- coming soon`} />
  </section>

  <!-- Explanation + Science -- page-specific content via slot -->
  <slot />

  <!-- CTA -->
  <FeatureCTA />
</BaseLayout>
```

Verify all 3 files with `npm run build && npx astro check`.
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/tuwa-website && npm run build 2>&1 | tail -5 && npx astro check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ScreenshotBlock.astro exists and contains `import { Image } from 'astro:assets'`
    - src/components/ScreenshotBlock.astro contains `interface Props` with `src?: ImageMetadata`
    - src/components/ScreenshotBlock.astro contains `role="img"` in placeholder div
    - src/components/FeatureCTA.astro exists and contains `import { APP_STORE_URL } from '../config'`
    - src/components/FeatureCTA.astro contains `<script is:inline>` with IntersectionObserver IIFE
    - src/components/FeatureCTA.astro contains `rel="noopener noreferrer"` on App Store link
    - src/components/FeatureCTA.astro does NOT contain `QRCode` or `qrcode`
    - src/layouts/FeaturePageLayout.astro exists and contains `import BaseLayout from './BaseLayout.astro'`
    - src/layouts/FeaturePageLayout.astro contains `import ScreenshotBlock from`
    - src/layouts/FeaturePageLayout.astro contains `import FeatureCTA from`
    - src/layouts/FeaturePageLayout.astro contains exactly one `<h1>` element
    - src/layouts/FeaturePageLayout.astro contains `<slot />`
    - src/layouts/FeaturePageLayout.astro contains `data-animate` on section elements
    - `npm run build` exits 0
    - `npx astro check` exits 0
  </acceptance_criteria>
  <done>ScreenshotBlock, FeatureCTA, and FeaturePageLayout created, build and type-check pass, all feature page infrastructure ready</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External links (App Store) | FeatureCTA links to external App Store URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | FeatureCTA.astro App Store link | mitigate | Include `rel="noopener noreferrer"` on `target="_blank"` link -- already established pattern from LandingCTA.astro |
| T-03-02 | Information Disclosure | Screenshot alt text | accept | Alt text describes UI state -- no PII or sensitive data in screenshot descriptions |
</threat_model>

<verification>
- `npm run build` succeeds with no errors
- `npx astro check` passes with no type errors
- All 3 new .astro files exist in correct paths
- ScreenshotBlock renders placeholder when no src provided (visible during build)
- FeatureCTA has no QR code (removed from LandingCTA adaptation)
- global.css has @plugin directive for typography
- chart.js appears in package.json dependencies
- 3 screenshot PNGs exist in src/assets/screenshots/
- 5 OG PNGs exist in public/og/ (one per feature page)
</verification>

<success_criteria>
All reusable infrastructure for feature pages is in place, including unique per-page OG images. Wave 2 plans can create individual pages by importing these components and passing page-specific content as props and slot content, referencing OG images at /og/{slug}.png.
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-pages/03-01-SUMMARY.md`
</output>

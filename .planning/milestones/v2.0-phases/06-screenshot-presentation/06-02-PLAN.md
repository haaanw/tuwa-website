---
phase: 06-screenshot-presentation
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/layouts/FeaturePageLayout.astro
  - src/layouts/CoachingPageLayout.astro
  - src/components/Hero.astro
autonomous: false
requirements:
  - SHOT-02
  - SHOT-04
  - SHOT-05
user_setup: []

must_haves:
  truths:
    - "All 5 feature pages display screenshots (or placeholders) inside a CSS iPhone device frame"
    - "The hero section displays the dashboard screenshot inside a device frame with subtle perspective tilt"
    - "Hero image loads eagerly with fetchpriority=high as the LCP element"
    - "Feature page screenshots load lazily with srcset for DPR-appropriate image selection"
    - "Pages without screenshots (coaching, cold-start) show a gradient placeholder inside the device frame"
  artifacts:
    - path: "src/layouts/FeaturePageLayout.astro"
      provides: "DeviceFrame import replacing ScreenshotBlock"
      contains: "DeviceFrame"
    - path: "src/layouts/CoachingPageLayout.astro"
      provides: "DeviceFrame import replacing ScreenshotBlock"
      contains: "DeviceFrame"
    - path: "src/components/Hero.astro"
      provides: "DeviceFrame with perspective wrapper and eager loading"
      contains: "perspective"
  key_links:
    - from: "src/layouts/FeaturePageLayout.astro"
      to: "src/components/DeviceFrame.astro"
      via: "import DeviceFrame"
      pattern: "import DeviceFrame from"
    - from: "src/layouts/CoachingPageLayout.astro"
      to: "src/components/DeviceFrame.astro"
      via: "import DeviceFrame"
      pattern: "import DeviceFrame from"
    - from: "src/components/Hero.astro"
      to: "src/components/DeviceFrame.astro"
      via: "import DeviceFrame"
      pattern: "import DeviceFrame from"
---

<objective>
Replace all ScreenshotBlock usage with the new DeviceFrame component across both layout files and the Hero section, completing the visual upgrade.

Purpose: This is the integration step that makes the DeviceFrame visible on every page. Feature page layouts swap the import, and the Hero gets a perspective-tilted DeviceFrame with eager loading for LCP. After this plan, every screenshot on the site renders inside a professional iPhone bezel.

Output: Updated FeaturePageLayout.astro, CoachingPageLayout.astro, Hero.astro + visual verification
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-screenshot-presentation/06-CONTEXT.md
@.planning/phases/06-screenshot-presentation/06-UI-SPEC.md
@.planning/phases/06-screenshot-presentation/06-PATTERNS.md
@.planning/phases/06-screenshot-presentation/06-01-SUMMARY.md

<interfaces>
<!-- DeviceFrame component created in Plan 01 -->

From src/components/DeviceFrame.astro (created in Plan 01):
```typescript
interface Props {
  src?: ImageMetadata;
  alt: string;
  placeholderLabel?: string;
  loading?: 'lazy' | 'eager';
  fetchpriority?: 'auto' | 'high' | 'low';
  widths?: number[];
  class?: string;
}
// Defaults: placeholderLabel='Coming soon', loading='lazy', fetchpriority='auto', widths=[320,640]
```

From src/layouts/FeaturePageLayout.astro (current state):
```astro
import ScreenshotBlock from '../components/ScreenshotBlock.astro';
<!-- line 51: -->
<ScreenshotBlock src={screenshot} alt={screenshotAlt} placeholderLabel={`${title} Screenshot — coming soon`} />
```

From src/layouts/CoachingPageLayout.astro (current state -- identical swap):
```astro
import ScreenshotBlock from '../components/ScreenshotBlock.astro';
<!-- line 51: -->
<ScreenshotBlock src={screenshot} alt={screenshotAlt} placeholderLabel={`${title} Screenshot — coming soon`} />
```

From src/components/Hero.astro (current state):
```astro
import { Image } from 'astro:assets';
import dashboardScreenshot from '../assets/screenshots/dashboard.png';
<!-- lines 44-52: Image with width={320} quality={90} format="webp" loading="eager" -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap ScreenshotBlock to DeviceFrame in layouts</name>
  <files>src/layouts/FeaturePageLayout.astro, src/layouts/CoachingPageLayout.astro</files>
  <read_first>
    - src/layouts/FeaturePageLayout.astro (current file -- find import and usage lines)
    - src/layouts/CoachingPageLayout.astro (current file -- identical swap pattern)
    - src/components/DeviceFrame.astro (verify Props interface from Plan 01)
  </read_first>
  <action>
**In `src/layouts/FeaturePageLayout.astro`:**

1. Replace the import on line 3:
   - REMOVE: `import ScreenshotBlock from '../components/ScreenshotBlock.astro';`
   - ADD: `import DeviceFrame from '../components/DeviceFrame.astro';`

2. Replace the component usage on line 51 (inside the `<!-- Screenshot -->` section):
   - REMOVE: `<ScreenshotBlock src={screenshot} alt={screenshotAlt} placeholderLabel={\`${title} Screenshot — coming soon\`} />`
   - ADD: `<DeviceFrame src={screenshot} alt={screenshotAlt} placeholderLabel="Coming soon" />`

3. Do NOT change anything else -- preserve `data-animate` on the section wrapper, preserve `padding-bottom: var(--space-3xl)`, preserve all other imports and layout structure.

**In `src/layouts/CoachingPageLayout.astro`:**

Identical changes:

1. Replace the import on line 3:
   - REMOVE: `import ScreenshotBlock from '../components/ScreenshotBlock.astro';`
   - ADD: `import DeviceFrame from '../components/DeviceFrame.astro';`

2. Replace the component usage on line 51:
   - REMOVE: `<ScreenshotBlock src={screenshot} alt={screenshotAlt} placeholderLabel={\`${title} Screenshot — coming soon\`} />`
   - ADD: `<DeviceFrame src={screenshot} alt={screenshotAlt} placeholderLabel="Coming soon" />`

3. Do NOT change anything else -- preserve all coaching-specific sections (coach-athlete slot, team-features slot, invite-flow slot).

**Why "Coming soon" instead of dynamic label:** Per D-04, placeholder label is fixed "Coming soon" text. The previous `${title} Screenshot -- coming soon` pattern is replaced with the simpler label since the device frame context already makes it clear what the placeholder represents.

**What happens automatically:** Pages that pass a `screenshot` prop (recovery-scoring, workload-tracking, smart-templates) will render the screenshot inside the DeviceFrame. Pages without a screenshot prop (coaching, cold-start) will render the gradient placeholder. No changes needed to any individual page files.
  </action>
  <verify>
    <automated>grep -c "DeviceFrame" src/layouts/FeaturePageLayout.astro && grep -c "ScreenshotBlock" src/layouts/FeaturePageLayout.astro && grep -c "DeviceFrame" src/layouts/CoachingPageLayout.astro && grep -c "ScreenshotBlock" src/layouts/CoachingPageLayout.astro && grep -c "data-animate" src/layouts/FeaturePageLayout.astro</automated>
  </verify>
  <acceptance_criteria>
    - FeaturePageLayout.astro contains `import DeviceFrame from '../components/DeviceFrame.astro'`
    - FeaturePageLayout.astro does NOT contain `ScreenshotBlock` anywhere
    - FeaturePageLayout.astro contains `<DeviceFrame src={screenshot} alt={screenshotAlt} placeholderLabel="Coming soon" />`
    - FeaturePageLayout.astro still contains `data-animate` on the screenshot section wrapper
    - CoachingPageLayout.astro contains `import DeviceFrame from '../components/DeviceFrame.astro'`
    - CoachingPageLayout.astro does NOT contain `ScreenshotBlock` anywhere
    - CoachingPageLayout.astro contains `<DeviceFrame src={screenshot} alt={screenshotAlt} placeholderLabel="Coming soon" />`
    - CoachingPageLayout.astro still contains all 3 named slots: `coach-athlete`, `team-features`, `invite-flow`
  </acceptance_criteria>
  <done>Both layout files import DeviceFrame instead of ScreenshotBlock. All 5 feature pages now render through DeviceFrame without any per-page file changes.</done>
</task>

<task type="auto">
  <name>Task 2: Update Hero.astro with DeviceFrame and perspective tilt</name>
  <files>src/components/Hero.astro</files>
  <read_first>
    - src/components/Hero.astro (current file -- find the Image block on lines 40-53)
    - src/components/DeviceFrame.astro (verify Props interface accepts loading, fetchpriority, widths)
    - .planning/phases/06-screenshot-presentation/06-UI-SPEC.md (hero frame enhancement specs)
  </read_first>
  <action>
**In `src/components/Hero.astro`:**

1. Add DeviceFrame import in the frontmatter (keep existing imports):
   ```astro
   ---
   import { Image } from 'astro:assets';
   import dashboardScreenshot from '../assets/screenshots/dashboard.png';
   import DeviceFrame from './DeviceFrame.astro';
   ---
   ```
   NOTE: The `{ Image }` import can be removed since DeviceFrame handles its own Image import. But keeping it is harmless -- remove it for cleanliness.

2. Replace the device mockup block (lines 40-53) with:
   ```astro
   <!-- Device mockup -- CSS iPhone frame with subtle perspective tilt (D-05) -->
   <div
     class="relative mx-auto"
     style="
       margin-top: var(--space-2xl);
       max-width: 320px;
       transform: perspective(1200px) rotateY(-4deg) rotateX(2deg);
     "
   >
     <DeviceFrame
       src={dashboardScreenshot}
       alt="Tuwa app showing today's recovery score of 82 -- HRV in green zone, sleep 7.5 hours."
       loading="eager"
       fetchpriority="high"
       widths={[320, 640, 960]}
     />
   </div>
   ```

**Critical details:**
- `transform: perspective(1200px) rotateY(-4deg) rotateX(2deg)` goes on the WRAPPER div, NOT on DeviceFrame. This is hero-only -- feature pages get flat frames. (Pitfall 4 from RESEARCH.md)
- `loading="eager"` and `fetchpriority="high"` are passed as props to DeviceFrame, which forwards them to the Image component. This is the LCP element (D-09).
- `widths={[320, 640, 960]}` -- hero gets 3 variants including 960px for 3x Retina. Feature pages only get `[320, 640]` (the DeviceFrame default).
- Use `dashboardScreenshot` (raw PNG) NOT any `_framed` variant. (Pitfall 5 from RESEARCH.md)
- Keep the existing `max-width: 320px` on the wrapper div.
- Remove the old `<Image>` tag with `width={320} quality={90} format="webp" class="rounded-[44px]"` entirely -- DeviceFrame handles all of this internally.

3. Do NOT change the headline, subtitle, or section padding. Only the device mockup block changes.
  </action>
  <verify>
    <automated>grep -c "DeviceFrame" src/components/Hero.astro && grep -c "perspective(1200px)" src/components/Hero.astro && grep "fetchpriority" src/components/Hero.astro && grep "loading=\"eager\"" src/components/Hero.astro && grep "widths={\[320, 640, 960\]}" src/components/Hero.astro && npx astro build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Hero.astro contains `import DeviceFrame from './DeviceFrame.astro'`
    - Hero.astro contains `import dashboardScreenshot from '../assets/screenshots/dashboard.png'` (NOT a _framed variant)
    - Hero.astro contains `transform: perspective(1200px) rotateY(-4deg) rotateX(2deg)` on the wrapper div
    - Hero.astro contains `<DeviceFrame` with `loading="eager"` and `fetchpriority="high"`
    - Hero.astro contains `widths={[320, 640, 960]}`
    - Hero.astro does NOT contain `width={320}` or `quality={90}` or `format="webp"` or `rounded-[44px]`
    - `npx astro build` completes without errors (all 10 pages generated)
  </acceptance_criteria>
  <done>Hero.astro renders dashboard screenshot inside DeviceFrame with perspective tilt. Hero image loads eagerly with high fetchpriority as LCP element. Build passes for all pages.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of device frames across all pages</name>
  <files>none (verification only)</files>
  <action>
Start the dev server with `npx astro dev` and present the verification checklist to the user. No code changes in this task -- human confirms visual correctness of the DeviceFrame integration completed in Tasks 1 and 2.
  </action>
  <what-built>
    DeviceFrame component has been integrated across the entire site:
    - Hero section: dashboard screenshot in tilted iPhone frame
    - recovery-scoring page: recovery screenshot in flat iPhone frame
    - workload-tracking page: workload screenshot in flat iPhone frame
    - smart-templates page: active-workout screenshot in flat iPhone frame
    - coaching page: gradient placeholder in flat iPhone frame
    - cold-start page: gradient placeholder in flat iPhone frame
  </what-built>
  <how-to-verify>
    1. Run `npx astro dev` and open http://localhost:4321
    2. **Landing page hero:** Verify the dashboard screenshot renders inside a dark iPhone bezel with subtle perspective tilt. The frame should have:
       - Dark (#1A1A1A) body with rounded corners
       - Dynamic Island pill at the top
       - Volume buttons on the left edge, power button on the right edge
       - Home indicator pill at the bottom
       - Slight 3D perspective (tilted slightly left and up)
       - Premium drop shadow
    3. **Feature pages** (visit each):
       - http://localhost:4321/features/recovery-scoring -- screenshot in flat (no tilt) iPhone frame
       - http://localhost:4321/features/workload-tracking -- screenshot in flat iPhone frame
       - http://localhost:4321/features/smart-templates -- screenshot in flat iPhone frame
    4. **Placeholder pages:**
       - http://localhost:4321/features/coaching -- warm gradient placeholder with "COMING SOON" text inside iPhone frame
       - http://localhost:4321/features/cold-start -- same gradient placeholder inside iPhone frame
    5. **Responsive check:** Resize browser window to mobile width (~375px) -- frames should shrink to 260px width. At tablet (~768px) -- 300px width. At desktop (~1280px) -- 320px width.
    6. **Retina check (if Retina display available):** Screenshots should appear crisp, not blurry. Open DevTools Network tab, reload a feature page, verify that the image request serves a WebP variant (not raw PNG).
  </how-to-verify>
  <verify>
    <automated>npx astro build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - User confirms hero device frame renders with perspective tilt and premium shadow
    - User confirms all 3 feature pages with screenshots show crisp images inside flat iPhone frames
    - User confirms coaching and cold-start pages show gradient placeholders inside iPhone frames
    - User confirms frames resize responsively across mobile/tablet/desktop viewports
  </acceptance_criteria>
  <done>User has visually confirmed that device frames render correctly on hero, all feature pages, and placeholder pages across responsive breakpoints.</done>
  <resume-signal>Type "approved" if frames look correct, or describe any visual issues (button placement, frame proportions, shadow intensity, placeholder appearance, etc.)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time image processing | Astro Image processes PNG screenshots at build time -- no user-uploaded content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | Layout file imports | accept | Import paths are developer-authored in .astro files, not user-controlled. Build would fail on invalid paths. |
| T-06-05 | Information Disclosure | Hero perspective CSS | accept | CSS transform values are purely visual -- no sensitive information exposed. |
</threat_model>

<verification>
- All 3 modified files import DeviceFrame correctly
- No file references ScreenshotBlock (except ScreenshotBlock.astro itself which is now unused)
- `npx astro build` completes without errors for all 10 pages
- Hero has perspective transform on wrapper div only
- Hero Image has loading="eager" and fetchpriority="high"
- Feature page Images use default lazy loading
</verification>

<success_criteria>
- Every feature page screenshot renders inside a CSS iPhone device frame
- Hero displays dashboard inside a perspective-tilted device frame
- Coaching and cold-start pages show gradient placeholders inside device frames
- Human verification confirms visual quality across hero, feature pages, and responsive breakpoints
</success_criteria>

<output>
After completion, create `.planning/phases/06-screenshot-presentation/06-02-SUMMARY.md`
</output>

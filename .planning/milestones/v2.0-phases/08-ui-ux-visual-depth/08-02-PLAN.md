---
phase: 08-ui-ux-visual-depth
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/components/Hero.astro
  - src/components/LandingCTA.astro
  - src/components/FeatureCTA.astro
  - src/components/Header.astro
  - src/components/Footer.astro
autonomous: true
requirements:
  - UIPX-01
  - UIPX-02

must_haves:
  truths:
    - "Every CTA button on the site scales up on hover and scales down on active press"
    - "Hero and CTA sections use consistent 128px desktop / 72px mobile vertical padding"
    - "Footer links change color on hover with smooth transition"
  artifacts:
    - path: "src/components/Hero.astro"
      provides: "Section spacing via .section-spaced class"
      contains: "section-spaced"
    - path: "src/components/LandingCTA.astro"
      provides: "Spacing + btn-cta on App Store badge"
      contains: "btn-cta"
    - path: "src/components/Footer.astro"
      provides: "nav-link class on footer links + btn-cta on Get the App"
      contains: "nav-link"
  key_links:
    - from: "src/components/LandingCTA.astro"
      to: "src/styles/global.css"
      via: "btn-cta class"
      pattern: "btn-cta"
    - from: "src/components/Footer.astro"
      to: "src/styles/global.css"
      via: "nav-link class"
      pattern: "nav-link"
---

<objective>
Apply spacing tokens and micro-interaction classes (btn-cta, nav-link) to all remaining interactive components: Hero, LandingCTA, FeatureCTA, Header, and Footer.

Purpose: Delivers UIPX-01 (consistent spacing) and UIPX-02 (hover micro-interactions) across all site-wide interactive elements.
Output: Five updated components with consistent spacing and hover states.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-ux-visual-depth/08-CONTEXT.md
@.planning/phases/08-ui-ux-visual-depth/08-PATTERNS.md
@.planning/phases/08-ui-ux-visual-depth/08-UI-SPEC.md
@.planning/phases/08-ui-ux-visual-depth/08-01-SUMMARY.md

<interfaces>
<!-- CSS classes available from Plan 01 (global.css) -->

.section-spaced — responsive section padding (72px mobile, 128px desktop)
.btn-cta — scale(1.02) on hover, scale(0.98) on active, 150ms ease-out
.nav-link — text-decoration: none; color transitions to var(--color-accent) on hover
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply spacing and btn-cta class to Hero, LandingCTA, and FeatureCTA</name>
  <files>src/components/Hero.astro, src/components/LandingCTA.astro, src/components/FeatureCTA.astro</files>
  <read_first>
    - src/components/Hero.astro (current state -- find section padding pattern to replace)
    - src/components/LandingCTA.astro (current state -- find App Store badge link and section padding)
    - src/components/FeatureCTA.astro (current state -- find App Store badge link and section padding)
    - .planning/phases/08-ui-ux-visual-depth/08-PATTERNS.md (exact before/after for each component)
  </read_first>
  <action>
**Hero.astro:**

Change the `<section>` tag (line 7-8) from:
```astro
<section
  class="px-6"
  style="padding-top: var(--space-3xl); padding-bottom: var(--space-3xl);"
>
```
To:
```astro
<section class="section-spaced px-6">
```
Remove the inline padding style entirely. The `.section-spaced` class handles responsive section spacing.

No btn-cta needed on Hero -- there is no CTA button in the Hero component (the download link is in LandingCTA).

**LandingCTA.astro:**

1. Change the inner `<div>` (line 21) from:
```astro
<div class="mx-auto max-w-6xl px-6" style="padding-top: var(--space-3xl); padding-bottom: var(--space-3xl);">
```
To:
```astro
<div class="section-spaced mx-auto max-w-6xl px-6">
```
Remove the inline padding style.

2. Add `btn-cta` class and `inline-block` to the App Store badge `<a>` (line 53). Change from:
```astro
<a
  href={APP_STORE_URL}
  target="_blank"
  rel="noopener noreferrer"
  aria-label="Download Tuwa on the App Store"
>
```
To:
```astro
<a
  href={APP_STORE_URL}
  target="_blank"
  rel="noopener noreferrer"
  aria-label="Download Tuwa on the App Store"
  class="btn-cta inline-block"
>
```

**FeatureCTA.astro:**

1. Change the inner `<div>` (line 10) from:
```astro
<div class="mx-auto max-w-6xl px-6" style="padding-top: var(--space-3xl); padding-bottom: var(--space-3xl);">
```
To:
```astro
<div class="section-spaced mx-auto max-w-6xl px-6">
```

2. Add `btn-cta` and `inline-block` to the App Store badge `<a>` (line 41). Change from:
```astro
<a
  href={APP_STORE_URL}
  target="_blank"
  rel="noopener noreferrer"
  aria-label="Download Tuwa on the App Store"
>
```
To:
```astro
<a
  href={APP_STORE_URL}
  target="_blank"
  rel="noopener noreferrer"
  aria-label="Download Tuwa on the App Store"
  class="btn-cta inline-block"
>
```
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/tuwa-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Hero.astro contains `class="section-spaced px-6"` on the section element
    - Hero.astro does NOT contain `padding-top: var(--space-3xl)` (old inline padding removed)
    - LandingCTA.astro contains `class="section-spaced mx-auto` on the inner div
    - LandingCTA.astro contains `class="btn-cta inline-block"` on the App Store badge link
    - FeatureCTA.astro contains `class="section-spaced mx-auto` on the inner div
    - FeatureCTA.astro contains `class="btn-cta inline-block"` on the App Store badge link
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>Hero, LandingCTA, and FeatureCTA use consistent section spacing and CTA buttons have scale micro-interaction on hover/active.</done>
</task>

<task type="auto">
  <name>Task 2: Apply btn-cta to Header CTA and nav-link to Footer links</name>
  <files>src/components/Header.astro, src/components/Footer.astro</files>
  <read_first>
    - src/components/Header.astro (current state -- find CTA button with onmouseover handlers, understand existing inline JS hover)
    - src/components/Footer.astro (current state -- find footer links with hover:underline, find Get the App button)
    - .planning/phases/08-ui-ux-visual-depth/08-PATTERNS.md (exact before/after for Header CTA and Footer links)
    - .planning/phases/08-ui-ux-visual-depth/08-RESEARCH.md (Open Question 2: minimal scope -- keep Header inline bg-color handlers, just add btn-cta for scale)
  </read_first>
  <action>
**Header.astro:**

Add `btn-cta` to the "Get the App" CTA button class attribute (line 80). Change from:
```astro
class="hidden md:inline-flex items-center transition-colors duration-100"
```
To:
```astro
class="btn-cta hidden md:inline-flex items-center transition-colors duration-100"
```

KEEP the existing `onmouseover` and `onmouseout` inline handlers that set `backgroundColor` and `boxShadow` -- they control different CSS properties from the `transform` that `.btn-cta` provides. Both work simultaneously without conflict per RESEARCH.md Open Question 2.

Do NOT modify the dropdown nav links (lines 57-61) or the Blog/Support links (lines 65-71). These already have working inline `onmouseover` color handlers. Per RESEARCH.md recommendation, full CSS migration of Header hover states is deferred to minimize scope. Phase 8 D-06 is satisfied by the existing inline handlers.

**Footer.astro:**

1. Add `btn-cta` to the "Get the App" CTA button (line 19). Change from:
```astro
class="inline-flex items-center gap-2"
```
To:
```astro
class="btn-cta inline-flex items-center gap-2"
```

2. Replace `hover:underline` Tailwind class with `nav-link` class on ALL footer feature links (lines 31-35) and resource/legal links (lines 43-44, 52-53). For each footer `<a>`:

Change from pattern:
```astro
<a href="/features/recovery-scoring" class="hover:underline" style="color: var(--color-text-2);">Recovery Scoring</a>
```
To:
```astro
<a href="/features/recovery-scoring" class="nav-link" style="color: var(--color-text-2);">Recovery Scoring</a>
```

Apply this change to all 9 footer links:
- `/features/recovery-scoring`
- `/features/workload-tracking`
- `/features/smart-templates`
- `/features/cold-start`
- `/features/coaching`
- `/blog`
- `/support`
- `/privacy`
- `/terms`

The `.nav-link` class provides `text-decoration: none` and `color: var(--color-accent)` on hover with a 100ms transition. This replaces the old `hover:underline` pattern with a color transition per D-06.
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/tuwa-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Header.astro contains `class="btn-cta hidden md:inline-flex` on the CTA link
    - Header.astro still contains `onmouseover` (existing bg-color handlers preserved)
    - Footer.astro contains `class="btn-cta inline-flex` on the Get the App link
    - Footer.astro does NOT contain `hover:underline` (replaced with nav-link)
    - Footer.astro contains `class="nav-link"` on at least 9 link elements
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>Header CTA has scale micro-interaction. Footer links have color transition hover and CTA has scale micro-interaction. All interactive elements on the site now have hover/active states per D-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Presentational CSS class additions only. No data flow or input surface changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | T (Tampering) | Header inline onmouseover | accept | Existing inline JS handlers are preserved as-is. No new inline JS added. btn-cta uses CSS-only transform. Low risk. |
</threat_model>

<verification>
1. `npm run build` passes
2. `grep -c "btn-cta" src/components/Header.astro` returns >= 1
3. `grep -c "btn-cta" src/components/Footer.astro` returns >= 1
4. `grep -c "nav-link" src/components/Footer.astro` returns >= 9
5. `grep -c "section-spaced" src/components/Hero.astro` returns >= 1
</verification>

<success_criteria>
- All CTA buttons site-wide have btn-cta class for scale micro-interaction
- All footer links use nav-link class for color transition hover
- Section spacing uses .section-spaced class on Hero, LandingCTA, FeatureCTA
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-ux-visual-depth/08-02-SUMMARY.md`
</output>

---
phase: 08-ui-ux-visual-depth
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/features/recovery-scoring.astro
  - src/pages/features/workload-tracking.astro
  - src/pages/features/smart-templates.astro
  - src/pages/features/cold-start.astro
  - src/pages/features/coaching.astro
  - src/pages/blog/index.astro
autonomous: true
gap_closure: true
requirements:
  - UIPX-01

must_haves:
  truths:
    - "Section spacing on feature pages uses 128px desktop / 72px mobile (matching landing page)"
    - "Section spacing on blog listing uses 128px desktop / 72px mobile (matching landing page)"
    - "No remaining inline padding-top/bottom: var(--space-3xl) on section wrappers in feature or blog pages"
  artifacts:
    - path: "src/pages/features/recovery-scoring.astro"
      provides: "Consistent section spacing via .section-spaced class"
      contains: "section-spaced"
    - path: "src/pages/features/workload-tracking.astro"
      provides: "Consistent section spacing via .section-spaced class"
      contains: "section-spaced"
    - path: "src/pages/features/smart-templates.astro"
      provides: "Consistent section spacing via .section-spaced class"
      contains: "section-spaced"
    - path: "src/pages/features/cold-start.astro"
      provides: "Consistent section spacing via .section-spaced class"
      contains: "section-spaced"
    - path: "src/pages/features/coaching.astro"
      provides: "Consistent section spacing via .section-spaced class"
      contains: "section-spaced"
    - path: "src/pages/blog/index.astro"
      provides: "Consistent section spacing via .section-spaced class"
      contains: "section-spaced"
  key_links:
    - from: "all 6 affected pages"
      to: "src/styles/global.css"
      via: ".section-spaced class"
      pattern: "section-spaced"
---

<objective>
Close SC-1 spacing gap: Apply .section-spaced class to all feature pages and blog listing page.

Purpose: The verification report found that 5 feature detail pages and the blog listing page still use inline `padding-top: var(--space-3xl)` (64px flat, no responsive breakpoint), which fails the UIPX-01 success criteria requiring 120-160px on desktop. The `.section-spaced` utility class (72px mobile / 128px desktop) is already defined in global.css and used on all landing page components. This plan applies it to the remaining 6 pages.

Output: All pages use consistent responsive section spacing via `.section-spaced`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-ux-visual-depth/08-VERIFICATION.md

<interfaces>
<!-- The .section-spaced class is already defined in global.css: -->

From src/styles/global.css:
```css
/* Phase 8 — Responsive section spacing (UIPX-01, D-03) */
.section-spaced {
  padding-top: var(--space-section-mobile);
  padding-bottom: var(--space-section-mobile);
}
@media (min-width: 768px) {
  .section-spaced {
    padding-top: var(--space-section-desktop);
    padding-bottom: var(--space-section-desktop);
  }
}
```

Where --space-section-mobile = 72px and --space-section-desktop = 128px.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply section-spaced to feature pages</name>
  <files>
    src/pages/features/recovery-scoring.astro
    src/pages/features/workload-tracking.astro
    src/pages/features/smart-templates.astro
    src/pages/features/cold-start.astro
    src/pages/features/coaching.astro
  </files>
  <read_first>
    src/styles/global.css
    src/pages/features/recovery-scoring.astro
    src/pages/features/workload-tracking.astro
    src/pages/features/smart-templates.astro
    src/pages/features/cold-start.astro
    src/pages/features/coaching.astro
  </read_first>
  <action>
    In each feature page, replace inline `style="padding-top: var(--space-3xl); padding-bottom: var(--space-3xl);"` with `class="section-spaced"` on section-level wrappers. Specific changes per file:

    **recovery-scoring.astro:**
    - Line 19: `<section style="padding-top: var(--space-3xl); padding-bottom: var(--space-3xl);">` -> `<section class="section-spaced">`
    - Line 92: `style="padding-top: var(--space-3xl); padding-bottom: var(--space-3xl);"` on the inner `<div>` -> add `class="section-spaced"` (merge with existing classes if present) and remove the padding-top/padding-bottom from the style attribute
    - Lines 44, 63, 73 (scroll-step divs): These are INTERNAL step spacing within the sticky showcase, NOT section-level spacing. Do NOT change these -- they control vertical rhythm between scroll steps within the sticky-showcase component, not inter-section gaps.

    **workload-tracking.astro:**
    - Line 18: `<section data-animate style="padding-top: var(--space-3xl); padding-bottom: var(--space-3xl);">` -> `<section data-animate class="section-spaced">` (keep data-animate, remove style, add class)
    - Line 74: inner div with `style="padding-top: var(--space-3xl); padding-bottom: var(--space-3xl);"` -> add `class="section-spaced"` and remove the padding style

    **smart-templates.astro:**
    - Line 17: `<section data-animate style="padding-top: var(--space-3xl); padding-bottom: var(--space-3xl);">` -> `<section data-animate class="section-spaced">`
    - Line 73: inner div -> add `class="section-spaced"` and remove padding style

    **cold-start.astro:**
    - Line 15: `<section data-animate style="padding-top: var(--space-3xl); padding-bottom: var(--space-3xl);">` -> `<section data-animate class="section-spaced">`
    - Line 75: inner div -> add `class="section-spaced"` and remove padding style

    **coaching.astro:**
    - Line 16: `<div class="mx-auto max-w-3xl px-6" style="padding-top: var(--space-3xl); padding-bottom: var(--space-3xl);">` -> `<div class="section-spaced mx-auto max-w-3xl px-6">` (add section-spaced to existing classes, remove style)

    IMPORTANT: When removing inline style, if the style attribute contains OTHER properties besides padding-top/padding-bottom (e.g., background-color, border), keep those other properties in the style attribute. Only remove the padding-top and padding-bottom declarations.
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/tuwa-website && grep -c 'section-spaced' src/pages/features/recovery-scoring.astro src/pages/features/workload-tracking.astro src/pages/features/smart-templates.astro src/pages/features/cold-start.astro src/pages/features/coaching.astro && grep -c 'padding-top: var(--space-3xl)' src/pages/features/recovery-scoring.astro src/pages/features/workload-tracking.astro src/pages/features/smart-templates.astro src/pages/features/cold-start.astro src/pages/features/coaching.astro</automated>
  </verify>
  <acceptance_criteria>
    - grep 'section-spaced' returns >= 1 match in each of the 5 feature page files
    - grep 'padding-top: var(--space-3xl)' returns matches ONLY in recovery-scoring.astro scroll-step divs (lines 44, 63, 73) -- zero matches in the other 4 feature files
    - No inline padding-top/padding-bottom: var(--space-3xl) on any section or div wrapper that serves as a page-level section boundary
  </acceptance_criteria>
  <done>All 5 feature pages use .section-spaced for section-level vertical spacing, matching the landing page pattern</done>
</task>

<task type="auto">
  <name>Task 2: Apply section-spaced to blog listing and verify build</name>
  <files>
    src/pages/blog/index.astro
  </files>
  <read_first>
    src/pages/blog/index.astro
  </read_first>
  <action>
    **blog/index.astro:**
    - Line 13: `<div style="padding-top: var(--space-3xl); padding-bottom: var(--space-3xl);">` -> `<div class="section-spaced">`

    After editing, run `npm run build` to verify the site still builds cleanly with no errors.
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/tuwa-website && grep -c 'section-spaced' src/pages/blog/index.astro && grep -c 'padding-top: var(--space-3xl)' src/pages/blog/index.astro && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep 'section-spaced' src/pages/blog/index.astro returns 1
    - grep 'padding-top: var(--space-3xl)' src/pages/blog/index.astro returns 0
    - npm run build completes successfully with no errors
  </acceptance_criteria>
  <done>Blog listing page uses .section-spaced and full site builds cleanly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — this is a CSS class swap on static pages.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | T (Tampering) | Static HTML output | accept | No user input, no dynamic content — class names are build-time only |
</threat_model>

<verification>
1. `grep -rn 'section-spaced' src/pages/features/ src/pages/blog/index.astro` shows matches in all 6 files
2. `grep -rn 'padding-top: var(--space-3xl)' src/pages/features/ src/pages/blog/` only returns matches inside scroll-step divs in recovery-scoring.astro (lines 44, 63, 73)
3. `npm run build` completes with exit code 0
4. `npx tsc --noEmit` passes (if applicable to .astro files)
</verification>

<success_criteria>
SC-1 gap is closed: all pages use .section-spaced for section-level vertical spacing, providing 128px on desktop and 72px on mobile — within the 120-160px desktop / 64-80px mobile range from the ROADMAP success criteria.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-ux-visual-depth/08-04-SUMMARY.md`
</output>

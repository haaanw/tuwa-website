---
phase: 08-ui-ux-visual-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/components/FeatureGrid.astro
autonomous: true
requirements:
  - UIPX-01
  - UIPX-03
  - UIPX-04
  - UIPX-05

must_haves:
  truths:
    - "Noise texture grain is visible on the page background at close inspection"
    - "Feature cards are arranged in a bento grid with Recovery Scoring spanning 2 columns and 2 rows on desktop"
    - "Feature cards lift with deeper shadow on hover"
    - "Section spacing tokens exist for consistent 128px desktop / 72px mobile gaps"
  artifacts:
    - path: "src/styles/global.css"
      provides: "All Phase 8 CSS tokens, noise texture, bento grid, hover classes"
      contains: "--shadow-card"
    - path: "src/components/FeatureGrid.astro"
      provides: "Bento grid layout with 3-tier visual hierarchy"
      contains: "bento-grid"
  key_links:
    - from: "src/components/FeatureGrid.astro"
      to: "src/styles/global.css"
      via: "bento-grid class + feature-card class"
      pattern: "bento-grid"
---

<objective>
Add all Phase 8 CSS foundation to global.css (design tokens, noise texture, bento grid, hover/interaction classes, spacing utility) and restructure FeatureGrid.astro from a uniform 3-column card grid to a CSS Grid bento layout with Recovery Scoring as the hero card.

Purpose: Establishes the CSS classes and tokens that all other Phase 8 plans consume, and delivers the highest-impact visual change (bento grid).
Output: Updated global.css with all Phase 8 additions; restructured FeatureGrid.astro with bento layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-ux-visual-depth/08-CONTEXT.md
@.planning/phases/08-ui-ux-visual-depth/08-RESEARCH.md
@.planning/phases/08-ui-ux-visual-depth/08-PATTERNS.md
@.planning/phases/08-ui-ux-visual-depth/08-UI-SPEC.md

<interfaces>
<!-- Key tokens and classes this plan creates, consumed by Plans 02 and 03 -->

From src/styles/global.css (to be added):
```css
/* New tokens */
--space-section-desktop: 128px;
--space-section-mobile: 72px;
--shadow-card: 0 1px 3px rgba(28,25,21,0.06), 0 1px 2px rgba(28,25,21,0.04);
--shadow-card-hover: 0 8px 24px rgba(28,25,21,0.10), 0 2px 8px rgba(28,25,21,0.06);
--shadow-bento-hero: 0 12px 40px rgba(28,25,21,0.12), 0 4px 12px rgba(28,25,21,0.07);
--shadow-bento-hero-hover: 0 20px 60px rgba(28,25,21,0.14), 0 6px 18px rgba(28,25,21,0.09);

/* New classes */
.section-spaced { ... }  /* responsive section padding */
.btn-cta { ... }          /* scale micro-interaction on hover/active */
.nav-link { ... }         /* color transition on hover */
.blog-listing-item { ... } /* shadow lift on hover */
.bento-grid { ... }       /* CSS Grid bento layout */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 8 CSS tokens, noise texture, and interaction classes to global.css</name>
  <files>src/styles/global.css</files>
  <read_first>
    - src/styles/global.css (current state -- understand existing token structure and media query patterns)
    - .planning/phases/08-ui-ux-visual-depth/08-PATTERNS.md (exact CSS snippets to add)
    - .planning/phases/08-ui-ux-visual-depth/08-UI-SPEC.md (authoritative token values and interaction contract)
  </read_first>
  <action>
Add the following to `src/styles/global.css` in the specified locations:

**1. New tokens in `:root` block (after line 61, before closing `}`):**
```css
/* Phase 8 — Section Spacing */
--space-section-desktop: 128px;
--space-section-mobile: 72px;

/* Phase 8 — Shadow Tokens (D-05) */
--shadow-card: 0 1px 3px rgba(28,25,21,0.06), 0 1px 2px rgba(28,25,21,0.04);
--shadow-card-hover: 0 8px 24px rgba(28,25,21,0.10), 0 2px 8px rgba(28,25,21,0.06);
--shadow-bento-hero: 0 12px 40px rgba(28,25,21,0.12), 0 4px 12px rgba(28,25,21,0.07);
--shadow-bento-hero-hover: 0 20px 60px rgba(28,25,21,0.14), 0 6px 18px rgba(28,25,21,0.09);
```

**2. Noise texture pseudo-element (after the `body` styles block, after line 72):**
```css
/* Phase 8 — Noise texture overlay (D-01, UIPX-04) */
body::after {
  content: '';
  position: fixed;
  inset: 0;
  pointer-events: none;
  z-index: 0;
  opacity: 0.025;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
  background-repeat: repeat;
  background-size: 256px 256px;
}
```

**3. Section spacing utility class (after noise texture):**
```css
/* Phase 8 — Responsive section spacing (UIPX-01, D-03) */
.section-spaced {
  padding-top: var(--space-section-mobile);
  padding-bottom: var(--space-section-mobile);
}
@media (min-width: 768px) {
  .section-spaced {
    padding-top: var(--space-section-desktop);
    padding-bottom: var(--space-section-desktop);
  }
}
```

**4. Button CTA micro-interaction class (after section spacing). Per D-04, MUST be inside reduced-motion guard:**
```css
/* Phase 8 — Button/CTA micro-interaction (D-04, UIPX-02) */
@media (prefers-reduced-motion: no-preference) {
  .btn-cta {
    transition: transform 150ms ease-out;
  }
  .btn-cta:hover {
    transform: scale(1.02);
  }
  .btn-cta:active {
    transform: scale(0.98);
  }
}
```

**5. Feature card hover shadow (replaces the scoped style that will be removed from FeatureGrid.astro). Per D-05, shadow depth only -- NO translateY:**
```css
/* Phase 8 — Feature card base + hover lift (D-05, UIPX-03) */
.feature-card {
  background-color: var(--color-surface);
  border: 1px solid var(--color-divider);
  border-radius: var(--radius-lg);
  padding: var(--space-md);
  text-decoration: none;
  display: flex;
  flex-direction: column;
  gap: var(--space-sm);
  box-shadow: var(--shadow-card);
}
.feature-card:hover,
.feature-card:focus-visible {
  background-color: var(--color-surface-el);
}
@media (prefers-reduced-motion: no-preference) {
  .feature-card {
    transition: box-shadow 200ms ease-out;
  }
  .feature-card:hover,
  .feature-card:focus-visible {
    box-shadow: var(--shadow-card-hover);
  }
}

/* Bento hero card (Recovery Scoring) — deeper shadow per D-07 */
.bento-hero-card {
  box-shadow: var(--shadow-bento-hero);
  padding: var(--space-xl);
}
@media (prefers-reduced-motion: no-preference) {
  .bento-hero-card:hover,
  .bento-hero-card:focus-visible {
    box-shadow: var(--shadow-bento-hero-hover);
  }
}
```

**6. Nav link hover class (for Footer links, per D-06):**
```css
/* Phase 8 — Nav link hover (D-06) */
.nav-link {
  text-decoration: none;
}
.nav-link:hover {
  color: var(--color-accent);
}
@media (prefers-reduced-motion: no-preference) {
  .nav-link {
    transition: color 100ms ease-out;
  }
}
```

**7. Blog listing item hover lift (per UIPX-03, Pitfall 5):**
```css
/* Phase 8 — Blog listing card hover lift (UIPX-03) */
.blog-listing-item {
  display: block;
  text-decoration: none;
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-card);
}
@media (prefers-reduced-motion: no-preference) {
  .blog-listing-item {
    transition: box-shadow 200ms ease-out;
  }
  .blog-listing-item:hover,
  .blog-listing-item:focus-visible {
    box-shadow: var(--shadow-card-hover);
  }
}
```

**8. Bento grid layout (after sticky-showcase section, per D-07/D-08/D-09):**
```css
/* Phase 8 — Bento grid layout (D-07, D-08, D-09, UIPX-05) */
.bento-grid {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: var(--space-xl);
  grid-template-areas:
    "recovery  recovery  workload"
    "recovery  recovery  templates"
    "cold-start cold-start coaching";
}
@media (min-width: 480px) and (max-width: 767px) {
  .bento-grid {
    grid-template-columns: 1fr 1fr;
    grid-template-areas:
      "recovery  recovery"
      "workload  templates"
      "cold-start coaching";
  }
}
@media (max-width: 479px) {
  .bento-grid {
    grid-template-columns: 1fr;
    grid-template-areas:
      "recovery"
      "workload"
      "templates"
      "cold-start"
      "coaching";
  }
}
```
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/tuwa-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - global.css contains `--space-section-desktop: 128px`
    - global.css contains `--space-section-mobile: 72px`
    - global.css contains `--shadow-card:`
    - global.css contains `--shadow-card-hover:`
    - global.css contains `--shadow-bento-hero:`
    - global.css contains `--shadow-bento-hero-hover:`
    - global.css contains `body::after`
    - global.css contains `feTurbulence`
    - global.css contains `opacity: 0.025`
    - global.css contains `pointer-events: none`
    - global.css contains `.section-spaced`
    - global.css contains `.btn-cta`
    - global.css contains `scale(1.02)`
    - global.css contains `scale(0.98)`
    - global.css contains `.feature-card` with `box-shadow: var(--shadow-card)`
    - global.css contains `.bento-hero-card`
    - global.css contains `.nav-link`
    - global.css contains `.blog-listing-item`
    - global.css contains `.bento-grid`
    - global.css contains `grid-template-areas`
    - global.css does NOT contain `translateY` inside any `.feature-card:hover` rule (D-05 forbids translate on cards)
    - Every `.btn-cta`, `.feature-card` transition, `.nav-link` transition, and `.blog-listing-item` transition is inside `@media (prefers-reduced-motion: no-preference)`
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>All Phase 8 CSS tokens, noise texture, utility classes, interaction classes, and bento grid layout are in global.css. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure FeatureGrid.astro from uniform grid to bento layout</name>
  <files>src/components/FeatureGrid.astro</files>
  <read_first>
    - src/components/FeatureGrid.astro (current state -- understand card DOM structure, stagger delays, scoped style block)
    - src/styles/global.css (verify .feature-card and .bento-grid classes exist from Task 1)
    - .planning/phases/08-ui-ux-visual-depth/08-PATTERNS.md (exact card restructure with grid-area assignments)
    - .planning/phases/08-ui-ux-visual-depth/08-UI-SPEC.md (bento grid contract, stagger delay remapping, hero card sizing)
  </read_first>
  <action>
Restructure `src/components/FeatureGrid.astro`:

**1. Remove the entire `<style>` block** (lines 1-19). The `.feature-card` class is now in global.css (Task 1).

**2. Remove inline section padding** from the `<section>` tag. Change:
```astro
<section style="padding-top: var(--space-3xl); padding-bottom: var(--space-3xl);">
```
To:
```astro
<section class="section-spaced">
```
Section spacing is now controlled by the `.section-spaced` class per UIPX-01/D-03. The component no longer owns its padding -- it uses the global utility class.

**3. Replace the `<ul>` grid classes.** Change:
```astro
<ul class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6" role="list">
```
To:
```astro
<ul class="bento-grid" role="list">
```

**4. Update each `<li>` with `grid-area` style and preserve `data-animate-delay` in visual reading order:**

Card 1 (Recovery Scoring -- hero card, D-07):
```astro
<li style="grid-area: recovery;" data-animate data-animate-delay="0ms">
  <a href="/features/recovery-scoring" class="feature-card bento-hero-card block">
```
Add `bento-hero-card` class (deeper shadow, larger padding per UI-SPEC hero card sizing). Remove `transition-colors duration-150` Tailwind classes from the `<a>` (transitions now handled by global CSS).

Card 2 (Workload Tracking):
```astro
<li style="grid-area: workload;" data-animate data-animate-delay="100ms">
  <a href="/features/workload-tracking" class="feature-card block">
```
Remove `transition-colors duration-150`.

Card 3 (Smart Templates):
```astro
<li style="grid-area: templates;" data-animate data-animate-delay="200ms">
  <a href="/features/smart-templates" class="feature-card block">
```
Remove `transition-colors duration-150`.

Card 4 (Cold-Start):
```astro
<li style="grid-area: cold-start;" data-animate data-animate-delay="300ms">
  <a href="/features/cold-start" class="feature-card block">
```
Remove `transition-colors duration-150`.

Card 5 (Coaching):
Remove the `class="md:col-start-2"` from the `<li>` (this old centering workaround is replaced by bento grid).
```astro
<li style="grid-area: coaching;" data-animate data-animate-delay="400ms">
  <a href="/features/coaching" class="feature-card block">
```
Remove `transition-colors duration-150`.

**5. Keep all card content (SVG icons, labels, titles, descriptions) exactly as-is.** Do not change any text, colors, or icon SVGs.
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/tuwa-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - FeatureGrid.astro does NOT contain a `<style>` block (scoped styles removed)
    - FeatureGrid.astro contains `class="bento-grid"` on the `<ul>` element
    - FeatureGrid.astro contains `class="section-spaced"` on the `<section>` element
    - FeatureGrid.astro does NOT contain `grid-cols-1` or `grid-cols-2` or `grid-cols-3` (old Tailwind grid classes removed)
    - FeatureGrid.astro does NOT contain `md:col-start-2` (old centering workaround removed)
    - FeatureGrid.astro does NOT contain `transition-colors duration-150` (hover transitions now in global CSS)
    - FeatureGrid.astro contains `grid-area: recovery` on first `<li>`
    - FeatureGrid.astro contains `grid-area: workload` on second `<li>`
    - FeatureGrid.astro contains `grid-area: templates` on third `<li>`
    - FeatureGrid.astro contains `grid-area: cold-start` on fourth `<li>`
    - FeatureGrid.astro contains `grid-area: coaching` on fifth `<li>`
    - FeatureGrid.astro contains `bento-hero-card` on the Recovery Scoring `<a>` element
    - FeatureGrid.astro contains `data-animate-delay="0ms"` through `data-animate-delay="400ms"` (stagger preserved)
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>FeatureGrid.astro uses bento grid layout with Recovery Scoring as hero card spanning 2 columns x 2 rows on desktop, 3-tier visual hierarchy (1 large + 2 medium + 2 small), and stagger animation delays preserved in visual reading order. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Phase 8 is purely presentational CSS and static component updates. No user input processed, no data flow changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | D (Denial of Service) | body::after noise texture | accept | CSS pseudo-element with SVG data-URI is rendered by browser GPU compositor; no network cost, no JS execution. Negligible performance impact. |
| T-08-02 | T (Tampering) | pointer-events: none on body::after | mitigate | body::after MUST have `pointer-events: none` to prevent the overlay from intercepting clicks on interactive elements below it. Verified in CSS. |
</threat_model>

<verification>
1. `npm run build` passes with no errors
2. `grep -c "bento-grid" src/styles/global.css` returns >= 1
3. `grep -c "body::after" src/styles/global.css` returns >= 1
4. `grep -c "bento-grid" src/components/FeatureGrid.astro` returns >= 1
5. `grep -c "grid-area:" src/components/FeatureGrid.astro` returns 5
</verification>

<success_criteria>
- All Phase 8 CSS tokens declared in global.css
- Noise texture renders site-wide via body::after
- Bento grid displays 3-tier card hierarchy on desktop
- Feature cards have shadow hover lift (no translateY)
- All transitions gated in prefers-reduced-motion
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-ux-visual-depth/08-01-SUMMARY.md`
</output>
